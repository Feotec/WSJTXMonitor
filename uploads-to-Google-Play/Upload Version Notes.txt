0.04-ads-03-15-2019-1907.aab - versionName "0.04", versionCode 1.  Release name is same as aab name.  First release, to internal testing.
  Code is archived as 03_15_2019_2018_WSJTXMonitor.zip.
0.11-ads-03-21-2019-2322.aab - versionName "0.11", versionCode 2.  Release name same as aab name.  Second release, also to internal testing.
  Code is archived as 03_21_2019_2322_WSJTXMonitor.zip.
0.14-ads-03-24-2019-1406.aab - versionName "0.14", versionCode 3.  Release name same as aab name.  Third release, also to internal testing.
  Code is archived as 03_24_2019_1406_WSJTXMonitor.zip.
1.01-ads-03-24-2019-1423.aab - versionName "1.01", versionCode 4.  Release name is 1.01-ads-03-24-2019-1406.  The time (1406) was a mistake.
  This was my first production release and is functinally identical to 0.14-ads-03-24-2019-1406.aab.  Code is 03_24_2019_1423_WSJTXMonitor.zip.
1.02-ads-03-25-2019-2104.aab - versionName "1.02", versionCode 5.  Release name is 1.02-ads-03-25-2019-2104.  This was a production release.
  I changed the "CQ Only" and "Keep Display On" menu items to include checkboxes.  Code archved as 03_25_2019_2104_WSJTXMonitor.zip.
1.03-ads-03-27-2019-1533.aab - versionName "1.03", versionCode 6.  Release name is 1.03-ads-03-27-2019-1533.  Production release.  I left a
  bug in the checkboxes of version 1.02, the check was not preserved if the screen was rotated.  Code archived as 03_27_2019_1533_WSJTXMonitor.zip.
1.08-ads-03-31-2019-1821.aab - versionName "1.08", versioncode 7.  Release name is 1.08-ads-03-31-2019-1821.  Production release.  This mostly
  simplified help menus.  Code is archived as 03-31-2019-1821_WSJTXMonitor.zip.
1.25-ads-04-08-2019-1527.aab - versionName "1.25", versioncode 8.  Release name is 1.25-ads-04-08-2019-1527.  Production release.  This rev added
  countries and grid squares to the main display.  It also had the 10 second scrolling freeze.  Code is archived as 1.25-ads-04-08-2019-1527.zip.
1.29-ads-04-15-2019-1054.aab - versionName "1.29", versionCode 9.  Release name is 1.29-ads-04-15-2019-1054.  Production release.  This rev added
  distance and azimuth to the main display.  Also added a feedback link.  Code is archived as 04_15_2019_1054_WSJTXMonitor.zip.
1.34-ads-07-07-2019-2132.aab - versionName "1.34", versionCode 11 (versionCode 10 was uploaded and then discarded).  Release name is 1.34-ads-07-07-2019-2132.
  Released to internal test.  This rev fixed two bugs concerning blank fields (DX call and my grid) in WSJT-X message 1.  In build.grade, compileSdkVersion
  and targetSdkVersion have both been bumped to 28.  Code is archived as 07_07_2019_2132_WSJTXMonitor.zip.

1.37-ads-07-15-2019-1158.aab - versionName "1.37", versionCode 12.  Release name 1.37-ads-07-15-2019-1158.  Full release.  Code is archived as 07_15_2019_1158_WSJTXMonitor.zip.
  Changes are primarily bug fixes and Spanish version.  Total list of changes:
    Spanish language version released.  All menus are in either language.  I did not translate the country names.
    Fixed two bugs UDPService.java.  First was if DX call was blank.  The Utf8 length word was -1 but I was using -1 to indicate error.  I substituted
      CONVERT_QINT_ERROR (-9999) as the new error indicator.  The second bug was if my grid was blank.  It would try to find the lon-lat of 'mm' instead of 'DM12mm'.
      The thread would quit.  This was fixed by not calling helperStationList.setMyGrid() (from UDPService.convertStatusMessage()) if string was blank.  Within
      helperStationList.setMyGrid() I also verified the integrity of the grid before getting lon-lat.
    In UDPService class all the String.format() use Locale.US instead of Locale.getDefault().  This was to prevent a faulty display in any language that
      didn't use arabic numbers.
    In manifest, set android:supportsRtl="false".  It had been true.
    UDPService class had two instances of hard-coded strings.  It was the notification message.  I made them both the same string and called it
      notification_string in strings.xml.
    dialog_port.xml, in the EditText I had a constraint referencing a view in another popup instead of the view below it.  I also changed a Left/Right parameter
      to a Start/End parameter.
    DialogNewFeatures class was moved into its own file (from MainActivity) because I needed a callback.  I needed the callback because when dismissed it had
      to do the same steps that About and EULA dialog do.  Also, within those three callbacks (in BaseActivity) before calling mService.dataIsFlowing() I checked
      that mService != null.
    In build.gradle, compileSdkVersion and targetSdkVersion bumped to 28 (Android 9.0).
    DebugUtils changed a great deal.  loggingDialogNewFeatures added (debug and release).  In debug version I added code to write log messages to a file on
      board the Android device, in Documents folder.  This meant that I had to add a call from within all logInfo() methods in all the classes.  Also, in MainActivity
      there are calls to DebugUtils.doStoragePermissions() and DebugUtils.closeAndroidLogFile().  Also, MainActivity has the onRequestPermissionsResult() callback.
      In release DebugUtils all file write functions are zeroed.  Manifest has the permissions commented out.

1.44-ads-08-02-2019-1958.aab - versionName "1.44", versionCode 13.  Release name 1.44-ads-08-02-2019-1958.  Full release.  Code is archived as 08_02_2019_1958_WSJTXMonitor.zip.
    (Important note - I moved past this revision before releasing.  Then I went back and made a couple small changes (added toast to filters and changed What's New text).  So
    there are a couple of archives of later code, v1.45 and v1.46 with same versionCode 13, but with dates before 08_02_2019_1958_WSJTXMonitor.zip).
  Changes:
    ads\res\values\bools.xml had a boolean value named "doing_ads".  Changed from true to false.  No need to keep the EEA stuff if no ads are being served.
    Fixed bug on "Keep display on" option.  I had two variables, one for MainActivity and another for DisplayAll.  Changed BaseActivity.screenAlwaysOnForDisplay
      to a static variable so there would be only one.
    Renamed Utils.java to WSJTXUtils.java.
    Added WSJTXUtils.getAndBumpNumberOfStartups().  This keeps track of the number of startups.  Stored in internal file.  For future use.  Accessed from BaseActivity.onCreate().
    Added WSJTXUtils.getAndUpdateNumberOfPackets().  This tracks total packets.  Stored in internal file.  For future use.  Accessed from UDPService.java.
    In DecodeDataStructure added an element continent.  Since can filter on continents I had to track the continent that the country is in.  One note is that Russia is both EU/AS.
    Added filters.  Added FiltersInterface.java (the user interface), DialogFiltersAlerts.java (generic select from list dialog, DialogPrefixCallsign.java (dialog for
      entering callsigns and prefixes), and Filters.java (the actual filter).
    Fixed bug in DisplayAll.onOptionsItemSelected().  When Home button selected I was calling Log.i(TAG..) instead of logInfo(), resultng in log file writing in release version.
    Fixed bug in HelperCountriesList.java.  All UA-UI (Russian) stations were listed as Europe instead of EU,AS.
    Country names are translated.  This meant several names shortened in English.  It also meant an explicit HelperCountriesList constructor because the Context needed to be
      passed so that the country names could be loaded from string_array_init.xml.
    Modified HelperCountriesList.getCountry() to return both country and continent.  Needed this so I could filter on continent.
    Added HelperCountriesList.getAllCountriesList().  It is called from FiltersInterface.java when the complete countries list is needed.  It is read from HelperCountriesList and sorted.
    HelperStationList constructor was modified to get the Context and pass it alog to HelperCountriesList constructor.  Previously HelperCountriesList constructor was called from
      HelperStationList.countries declaration.
    Modified HelperStationList.verifyGridSquare() to verify 6 digit grids.  However, still only a 4 digit grid designation.  This was because WSPR often got 6 digit grids and the
      result was no distance or azimuth info.
    ReturnStationData.java had a continent and callsign elements added.  This was for filtering on continent and callsign/prefix.
    HelperStationList.java private structure StationData has the continent added, so can filter on continent.  HelperStationList.checkStation() modified to return continent in
      ReturnStationData structure.
    MainActivity.java modified to accomodate filters:
      1) filtersInterface class element added and mRetainedFragment element, onResumeFragments() method to store it during screen rotations.
      2) onCreateOptionsMenu() changed to add a filters option (it's MenuItem misnamed as alertsItem).
      3) added 8 listeners, 4 for DialogFiltersAlerts.java and 4 for DialogPrefixCallsign.java.  All 8 call into FiltersInterface.java.
    UDPService.java modified to accomodate filters:
      1) filterObject added.
      2) filters() method added.  Called by MainActivity menu to indicate whether filterObject is null or not.
      3) buildBurstAndAppend() modified to actually do the filtering.
      4) convertWSPRDecodeMessage() modified to store continent in decodeData structure
      5) convertDecodeMessage() modified to store continent and callsign in decodeData structure.
      6) fixed bug in convertQint32ToInt() and convertQint64ToInt().  If message had exactly 4 (or 8) bytes left it attempted to read more bytes instead of stopping there.
    button_colors.xml modified to allow greyed out dialog buttons.  I didn't have any greyed out buttons until I did filters.  This xml was referenced in styles.xml.
    strings.xml (English and Spanish) modified to add text for FiltersInterface dialogs.
    Web page help files also added for Filters.

1.51-ads-08-20-2019-1648.aab - versionName "1.51", versionCode 14.  Release name 1.51-ads-08-20-2019-1648.  Full release.  Code is archived as 08-20-2019-1648_WSJTXMonitor.zip.
  Changed:
    Added four new classes, Alerts, AlertsInterface, DialogAlertSettings, and DialogMinSignal.  There is also a TouchableTextView class which was meant to handle the places
      where I'm doing a touch screen callback.  However, the callback doesn't get called in Android 4.4 so I'm not using it.  See WSJTX Monitor Notes.docx, 8/7/2019.
      DialogAlertSettings, and DialogMinSignal also have layout files, dialog_alert_settings.xml and dialog_min_signal.xml.
    BaseActivity had a call to UDPService.setAlerts().  That method no longer exists.  It also calls (from .onCreate()) a static method in Alerts class, .initializeChannelIds().
    A bug was present regarding BaseActivity.screenAlwaysOnForDisplay.  It was made a static variable in v1.44.  However, because it is static then, if set at shutdown, it will
      be set again at startup.  I fixed the bug by setting it false in BaseActivity.onDestroy(), conditioned on isFinishing().
    In DecodeDataStruct added an element named isAlert.  This is used in UDPService.
    DialogFilterAlerts and DialogPrefixCallsign added a parameter doingFiltersParam.  This value is passed to the listeners so that they know whether to call methods in
      FiltersInterface or AlertsInterface.
    DialogNewFeatures added an onStart() method override because I needed to add a link in the dialog.
    In Filters removed method initializationSuccessful() since it was never called.
    In FiltersInterface fixed a bug.  The Toast message that said what filter was enabled was not translated.  It is now.  The only other changes was to pass "true" to the
      doingFilters parameter and change the values names read into endOfLargeFont (they were renamed in string_array_init.xml).
    HelperCountriesList - I documented the changes (at top of countriesList[] array) that I made to string_array_init.xml.
    MainActivity
      1) Implemented listeners for DialogAlertSettings and DialogMinSignal.
      2) Added an AlertsInterface object, alertsInterface, and created a RetainedFragment to store it in named RetainedFragmentAlert.  Renamed the RetainedFragment for
         FiltersInterface to RetainedFragmentFilter.
      3) In onCreateOptionsMenu() removed MenuItem alertsItem from being conditioned on DebugUtils.alertsEnabled (that boolean is no longer needed).  Same thing in
         onPrepareOptionsMenu().  In onOptionsItemSelected(), when the MenuItem action_alerts is selected, alertsInterface object is either created is it doesn't exist or
         alertsInterface.startUI() is called if it does.  If an alert is on (mService.alerts() == true) then mService.alertObject is set to null, turning alerts off.
      4) In onResume(), if (initialStartup == false) AND (mService != null) AND (mService.dataIsFlowing() == true) then call manageDisplay().  This was done so that the display
         would be updated after each screen rotation or if app was returning from background.  I found that if an alert occured when main display is not up then it could
         take a few seconds (until next burst) before I saw the cause of the alert.
      5) In onResumeFragments() added code to save RetainedFragmentAlert.  Similarly, in onStop() stored RetainedFragmentAlert.
      6) DialogPrefixCallsign and DialogFilterAlerts listeners use the new doingFilters parameter to call appropriate function in AlertsInterface and FiltersInterface.
      7) In MainDisplayHelp static class, added code to support message that says that yellow indicates an alert.
    UDPService
      1) Added an AlertObject named alertObject.
      2) Added code to support ringtone, vibration, and (API 26+) four notification channels.  Most of this was done in onCreate().
      3) getNotification() changed.  Previously it was only passed the notification string.  That string is now an ArrayList<CharSequence>, since the notification can be several
         lines.  Also, a boolean is passed named alertThisTime.  This allows me to keep a notification displayed until the next one comes along.  Even though the notification
         continues to be displayed the audible alert is done only once for each alert.  alertThisTime is used to indicate that also.  It also gets the selected notification channel
         from alertObject and sets the sound URI and vibration settings appropriately.  Finally NotificationCompat.Builder is called with these changes, including using InboxStyle
         for the notification text display.  This was chosen because it wouldn't wrap around long lines.
      4) Method alerts() returns a boolean if alertObject != null.
      5) testVibrationAndSound() added.  Called from AlertsInterface, Alerts Options menu.  createRingtone() (another new method) is called if a new ringtone is created.
      6) newAlertSetRingtone() added.  This is called from AlertsInterface when a new alert is created.  If API 26+ it will read the ringtone from the selected channel and set
         the MainActivity ringtone to be the same.  If a new ringtone is added  then createRingtone() is called.
      7) mainActivityVibrate() and mainActivitySound() are added.  These are called (from UDPService.udpLoop()) to make sound/vibrate when main display is up.
      8) In buildBurstAndAppend() the filter code was removed and placed in convertWSPRDecodeMessage() and convertWSPRDecodeMessage().  In those two methods filterObject is checked
         and if the filter blocks the current message then the data is not added to the linked list.  This is more efficient than removing items from linked list in buildBurstAndAppend().
      9) Also in buildBurstAndAppend(), the function now returns an ArrayList<CharSequence> which is passed to getNotification().
     10) As mentioned in #8, convertWSPRDecodeMessage() and convertWSPRDecodeMessage() now have filter called.  Alert code is also called there and DecodeDataStruct.isAlert set.
     11) In convertDecodeMessage() I built a work-around for a WSJT-X bug.  It seems that sometimes WSJT-X will send out unusually long utf8 messages.  They have the correct message
         plus many spaces and 'a1' at the end.  They also seem to appear at intervals other than the normal 15 sec.  An example is captured in capturedPackets_Bug_20190816.dat (in
         this archive).  Anyhow, I look for three spaces and truncate the message there.
   WSJTXUtils - added code to count the number of alerts used, getAndBumpNumberOfAlertUses().  It is called from AlertsInterface when a new alert is created or an old one is recalled.
   dimens.xml - added this file for English and Spanish because I needed to specify a different text size in DialogAlertSettings.  This is used in dialog_alert_settings.xml.
   string_array_init.xml - added string-array items to support AlertInterface.  Also renamed the similar items that supported FilterInterface.  Also made a few changed to the
     the countries list, as documented in HelperCountriesList above.  This is the English version.  The Spanish version had the same except I didn't change the countries list.
   strings.xml - added strings to support alerts and "New in Version" dialog. (English and Spanish)
   AndroidManifest.xml - Added VIBRATE permissions.
   DebugUtils - added debug booleans for the new classes.
   Chinese string.xml added.  Only the setup instructions are in Chinese.

1.54-ads-08-23-2019-1700.aab - versionName "1.54", versionCode 15.  Release name 1.54-ads-08-23-2019-1700.  Full release.  Code is archived as 08_23_2019_1700_WSJTXMonitor.zip.
  Changed:
    Added one new class, DialogSorting.java, with three listeners for help, cancel, and finish.
    To accomplish sorting I added two new elements to DecodeDataStruct, distanceInKmNum and azimuthNum (both doubles), because distance and azimuth were stored as a string.  These elements
      were initialized to -1.0 in the constructor.  It also required to override the compareTo() method.  This is used for SNR.  For frequency I embedded a static class, FreqComparator.  For
      distance an embedded static class DistanceComparator.  And for azimuth a static class AzimuthComparator.
    ReturnStationData class added the same two data elements, distanceInKmNum and azimuthNum, and initialized them to -1.0 in the constructor.  I initialized the callsign element to "".
    In HelperStationList class I added identical data elements (distanceInKmNum and azimuthNum) to the embedded class StationData.  I also added a constructor for that embedded class which
      initialized the two added elements to -1.0.  (Without that the return value didn't have them set to -1.0 if there was no grid square).  Within the checkStation() method I filled in
      those two new elements.  Unrelated, in debugDumpHistory() I wrote to the log file outside of the local logInfo() method since debugDumpHistory() was only called from UDPService.
    MainActivity implemented the three listeners from DialogSorting.  I also added a menu item for sorting.  Finally, I added a toast() call for filters, alerts, and sorting when that
      option is turned off.
    UDPService implemented sorting.  A boolean, sorting, and an enum sortingOn were added.  Then enum had SORTING_NONE, SORTING_SNR, SORTING_FREQ, SORTING_DISTANCE, and SORTING_AZIMUTH.
      The DialogSorting finish listener calls UDPService.setSorting() method when sorting is turned on.  It is passed the value for the boolean (sorting) and an integer which is used to
      set the enum (sortingOn).  Not the most elegant method.  The setSorting() method is the interface to turn sorting on and off.  Also, an interface method, doingSorting(), returns the
      status of the boolean.  UDPService.buildBurstAndAppend() was modified quite a bit.  Sorting is inefficient with a linked list (so I've read).  So I converted decodeDataList to an
      array (decodeDataArray[]).  I then modified buildBurstAndAppend() to work with the array instead of the linked list.  I also added some timing variables to print out how long
      buildBurstAndAppend() took to execute.  (I left them in.  Hopefully they will be optimized out).  The two new DecodeDataStruct elements, distanceInKmNum and azimuthNum, are filled
      in when msg 2 or 10 arrives.
    UDPService also had a bug, which was there all along.  Normally the app starts up in between bursts.  A boolean, dataMsg2or10Flowing, is set when either msg 2 or 10 has occured.  It is
      set once and never cleared.  If clear then UDPService will send a broadcast to the main display when any message appears.  Once set it will only do that when a burst ends.  However,
      if the app starts in the middle of a burst then it won't give the user an indication that a WSJT-X connection has been made until the end of the next burst.  I fixed that bug by
      setting decodeFlagMirror when msg 2 or 10 arrives.  Previously it was only set when msg 1 (status) indicates that a decode has begun.  That code is still there but it may be unnecessary.
    In strings.xml new strings were added to support sorting.
    DebugUtils - added debug boolean for the new DialogSorting class.

1.68-ads-08-28-2019-1502.aab - versionName "1.68", versionCode 20.  Release name 1.68-ads-08-28-2019-1502.  Beta release.  Code is archived as 08_28_2019_1502_WSJTXMonitor.zip (called v1.63 there).
      Released to beta because I was unsure of some of the issues with Google Maps API key and the transition to androidx.  This was supposed to be v1.63 version code 16 but Google Play
      issued me a warning.  I uploaded (but did not release) several test versions to determine that the cause of the warning was the addition of Google Maps.  See WSJTX Monitor Notes.docx 8/30/3019.
  Changed:
    Transitioned to androidx.  This meant changes to most source files.  The following had no change other than the import statements at the top of the file.
          AlertsInterface                      DialogSetup                         dialog_alert_settings.xml
          DialogAbout                          DialogSetupDetails                  dialog_ipport.xml
          DialogAlertSettings                  DialogSorting                       dialog_min_signal.xml
          DialogEULA                           DisplayAll                          dialog_port.xml
          DialogFilterAlerts                   FiltersInterface                    dialog_setup_details.xml
          DialogMinSignal                      TouchableTextView                   dialog_setup_details_jtalert.xml
          DialogNewFeatures                    activity_display_all.xml            main_toolbar.xml
          DialogPort                           activity_main.xml                   build.gradle (both versions.  app version also had versionCode/Name changed)
          DialogPrefixCallsign                 dialog_about.xml
    In src/debug/res/values/ and src/release/res/values the file google_maps_api.xml was added.
    MapsActivity added
    activity_maps.xml added
    menu_maps.xml added
    menu_main.xml was modified to add the mapping menu item (in comments)
    In DebugUtils.java loggingMapsActivity was added.
    In BaseActivity.java there were four places where DialogSetup (the dialog that said there is no connection between the app and WSJT-X) was invoked.  I added .setCancelable(false) to each.
    HelperStationList was modified to pull the user's grid square and the grid square of the last 30 stations received.  This involved:
      To StationData class added mapNumber and a compareTo() method.  It also implements Comparable interface.
      Added doFilterForMapping() method.  It set StationData.mapNumber element based on whether the gridSquare is filled in and whether the station is now filtered out.
      Created a private variable lastStationUsed.  This was set to the last StationData structure accessed in checkStation() method.  It is used by doFilterForMapping() so that it doesn't
        have to search through the entire stationList linked list.
      Added getRxGridsForMapping().  This returns a string array of the last 30 stations heard, excluding those without a known grid square and that are currently filtered out (both determined
        based on the value of mapNumber).
      Small additions to the debug lines in debugDumpHistory()
    In MainActivity the Mapping menu item was added.  It gets the last 30 grid squares and the user's grid and invokes MapsActivity, passing the grids as an Extra in the intent.
    In UDPService added a method to return a pointer to the HelperStationList class used (for use by MainActivity so it can get the grid squares and pass them to MapsActivity via an intent).
      Also modified convertWSPRDecodeMessage() and convertDecodeMessage() to call helperStationList.doFilterForMapping(), passing the boolean indicating if the station is filtered.
    colors.xml added colorPrimaryMaps and colorTextMaps.  These were to color the action bar in MapsActivity so that they matched the action bar in the other two activities.
    styles.xml added AppMapTheme and a style for AppMapTheme named ThemeOverlay.AppTheme.ActionBar.
    ids.xml added action_mapping id.
    strings.xml (English, Spanish, and Chinese) added text to support the mapping menu item, MapsActivity action bar text, and the three dialogs within MapsActivity.
    AndroidManifest.xml modified to add the MapsActivity and its API key.  The conversion to androidx also rearranged a few things.  Inserting the MapsActivity also added ACCESS_FINE_LOCATION
      which I commented out.

1.71-ads-09-01-2019-1105.aab - versionName "1.71", versionCode 23.  Release name 1.71-ads-09-01-2019-1105.  Full release.  Code is archived as 09_01_2019_1105_WSJTXMonitor.zip.
  Primary change was that I'm forcing new installations to go through the DialogSetupDetails series until data flows.  After data has flowed then it behaves the same as before.
  Since v1.68 was only released to internal test I'm comparing against zOldVersions/WSJTXMonitor_v1.70_nomaps/.
    BaseActivity is where most changes were.
      Created two new boolean variables: dataNeverFlowed (protected) and dialogSetupDetailsDoCancel (private)
      onCreate() set dataNeverFlowed if new installation first startup (versionCodeRead == 0).  Wrote the variable back to shared preferences.
        If not new installation cleared dataNeverFlowed and wrote to shared pref (I don't want this on an upgrade).
        If regular startup (versionCode read from shared pref == real versionCode) then read dataNeverFlowed from shared pref.
      dataNeverFlowed is written and read with onSaveInstanceState() and updateValuesFromBundle().
      Created manageDialogSetup().  It checks dataNeverFlowed and, if set, goes right to manageDialogSetupDetails() which invokes DialogSetupDetails sequence.  If clear it does the usual
        process.  manageDialogSetup() is called when About or EULA menus are closed (dialogAboutPositiveClick() and dialogEULAOkClick() just above it).
      dialogSetupAskForDetails() sets dialogSetupDetailsDoCancel which allows the user to cancel out of DialogSetupDetails sequence.
      Within manageDialogSetupDetails()
        DialogSetupDetails .setCancelable() set false.
        dialogSetupDetailsDoCancel passed to DialogSetupDetails (called parameterDoCancel there)
    MainActivity manageDisplay() checks dataNeverFlowed.  If set it clears it and writes value back to shared pref.
    DialogSetup only moved newInstance() method to top of file.  No functional changes.
    DialogSetupDetails added parameterDoCancel and conditioned the cancel button on it being true.  Also added onResume() method to capture back key (hardware) and have it call the same
      listener as the back button soft key.
    WSJTXUtils added two methods to get and write dataNeverFlowed variable, getDataNeverFlowed() and setDataNeverFlowed().
    dialog_setup_details.xml made small adjustments to margins to make things fit better (on step 1 some extra words were written)
    strings.xml changed header_text_step1 to explain to user why he's doing this.  Also jtalert_header_text was changed to mention that computer and phone must be on the same network.

1.77-ads-09-12-2019-1233.aab - versionName "1.77", versionCode 24.  Release name 1.77-ads-09-12-2019-1233.  Full release.  Code is archived as 09_12_2019_1233_WSJTXMonitor.zip.
  Primay change was the addition of Reply feature.
  Compared against release v1.71.
    Several drawables were added. Reply icons - baseline_reply_black_36.png and baseline_no_reply_black_36.png were added to all the drawable-*/ screen size directories.  In the
      drawable/ directory baseline_reply_black_48.png was added for the DialogReplyHelp and DialogReplyPrehelp dialogs.  In same directory reply_setup_step3.png and reply_setup_step3_sm.png
      was added to support detailed step-by-step setting up of replies.  (The first two images were same as original setup, only the third needed to be different).
    ReplyDialogHelp.java and its layout file reply_dialog_help.xml were added.  Dialog is displayed after long press on reply icon.  Logging boolean added to DebugUtils.java.
    ReplyDialogPreHelp.java and its layout file reply_dialog_prehelp.xml were added.  Dialog displayed after an alert is created or recalled.  Had "Do not show again" button.  Logging boolean added.
    strings.xml had text added to support the new dialogs and to support the different "Waiting ..." message that is displayed if data never flowed.
    main_menu.xml added to help icon the attribute android:orderInCategory="90".  This was so that the Reply icon (set to "100" in Java) always appeared to the left of the Help icon.
    WSJTXUtils had two new functions added, getReplyHelpDontShowAgain() and setReplyHelpDontShowAgain().  This stores the DialogReplyPreHelp "Don't show again" checkbox status.
    UDPService added several data elements to support Replies.
            msg4SNR - Initialized to NO_SNR (-999).  Stores the SNR of the candidate reply.  If another alert occurs when a greater SNR this it will replace the candidate reply.
              It is also used as a flag.  A reply will not be sent out unless msg4SNR != NO_SNR.
            msg4Length - set to length of reply.  Used only in udpLoop() when building DatagramPacket to send to WSJT-X.
            msg4Enabled - the main boolean indicating that Replies are on or off.  Set and cleared by interface methods doingReply() and setDoingReply().  A reply will not be sent if
              msg4Enabled == false or if (msg4Enabled == true and msg4SNR != NO_SNR and msg4Inhibit == 0).  Elsewhere it is used to condition the loading of a reply and also used to set
              msg4Inhibit.
            msg4Inhibit - a counter that prevents sending a reply until two bursts after a transmission.  Initialized to zero.  Counted down in udpLoop() in "if (endOfBurst)" section.
              Set to MSG4_INHIBIT_COUNT (2) in convertStatusMessage() if TxEn is true.
            msg4Buffer - where the reply message is built up from.  The reply, after the header bytes, must contain a duplicate of most of the contents of msg2.  Those bytes are loaded
              in convertDecodeMessage().
      onCreate() initialized these data elements, in particular the msg4Buffer[] since several bytes in that buffer never change.
      getNotification() had the block of code that accessed alertObject conditioned on alertObject != null.  Should never happen, just a precaution.
      Interface methods doingReply() and setDoingReply() were added.  The first returns the status of UDPService.msg4Enabled boolean.  The second sets it and reinitializes Reply data elements.
        If alertObject == null setDoingReply() always sets msg4Enabled = false.
      udpLoop(), "if (endOfBurst)" has a block of code beginning with "if (msg4Enabled)" added.  It checks that msg4Enables == true, if msg4Inhibit > 0 it counts down and does nothing.  If
        msg4Inhibit == 0 then it reinitializes msg4SNR to NO_SNR and sends the packet to WSJT-X.
      The call to helperStationList.checkStation() has last boolean set to false when called from convertWSPRDecodeMessage() and to isMyCall when called from convertDecodeMessage().
      In convertWSPRDecodeMessage() and convertDecodeMessage() the call to helperStationList.doFilterForMapping() is conditioned out.  It is a mapping feature only so since mapping is off there is no need.
      In convertDecodeMessage() the data elements are set for a reply if alerts are on and all data variables are set.  This is where msg4Buffer[] is built up.
    MainActivity
      Added support for DialogReplyHelp.dialogReplyHelpListener().  This was needed to initiate step-by-step WSJT-X setup instructions.
      Different text was used for "Waiting ..." text based on dataNeverFlowed boolean.
      In onCreateOptionsMenu() code was added to display a Reply icon and to support long press on Reply icon.  This involved making the normal touch of a Reply icon to use a callback (defined
        withing onCreateOptionsMenu()) instead of the normal method in onOptionsItemSelected().  (The code in onOptionsItemSelected() is there but commented out).
      onPrepareOptionsMenu() saved the value of menu parameter and passed it to alertsInterface.  AlertsInterface is the one to change the icon as alerts are turned on and off so it needs
        a reference to it.  Also, since it will change as the screen is rotated it has to be passed to AlertsInterface each time the screen is rotated.
        onPrepareOptionsMenu() also is responsible for making the Reply icon invisible and disabled if no alerts and visible and enable if so.  It also sets the correct icon based on
        whether Replies are on or off.
      onOptionsItemSelected(), in "case action_alerts" block it will disable and make invisible the reply icon if alserts are turned off.  Also, will pass mainMenu reference to AlertsInterface constructor.
      onPause() had call to super.onPause() moved to bottom of method.  This makes no difference.  It was changed when a user said the app didn't shut down properly.
    HelperStationList
      In static class StationData, an boolean replyWorkedAlready was added and initialized to false.  This is used to prevent building a reply to a station that has already been worked.
      In constructor a boolean isMyCall was added to the parameter list.  It is used to set (but never clear) replyWorkedAlready element in StationData.  Also in constructor, redid the
        code that verifies the callsign.  I changed the series of if/else to single if statements since I want the all to be considered.  I also removed the if CQ block because the next
        block that checks if numSubStrings == 2 will catch that.  That numSubStrings == 2 was added to fix a bug where a message like "SV1CIF W5PR" would set callsign to SV1CIF instead of W5PR.
      stringContainsNumberAndLetter() was added to make sure the callsign only contained letters, numbers, and the two slash marks.  Previously it only checked that it contained letters and numbers.
      stationHasBeenWorked() method added, returns replyWorkedAlready element of last station checked, referenced by lastStationUsed.  (lastStationUsed was added to support mapping).  This method
        is called from UDPService.convertDecodeMessage() as one of conditions on building a reply.
    DisplyAll had different text used for "Waiting ..." text based on dataNeverFlowed boolean.
    AlertsInterface had data element mainMenu added, used to store reference to menu since it changes with each screen rotation.
      Constructor has an additional parameter to set mainMenu data element.
      setMainMenu() method added to set mainMenu data element from MainActivity.onPrepareOptionsMenu().
      Private method doReplyIcon() was added to enable/make visible or disable/make invisible the reply icon based on the parameter "enabled".  It also sets the reply icon to be
        baseline_reply_black_36 since replies are always off when alerts are created.
      Private method doReplyHelpDialog() will bring up DialogReplyPreHelp after alert created unless user previously checked the "Don't show again" checkbox (indicated by
        WSJTXUtils.getReplyHelpDontShowAgain() returning true or false).
      doNext() will call doReplyHelpDialog() and doReplyIcon( true ) at the two exit points of the alerts (when alerts are either created or recalled).
      doCancel() calls doReplyIcon( false ) when the user cancels an alert.
    BaseActivity had code added to support the Reply step-by-step setup instructions.  I reused BaseActivity.DialogSetupDetails() method.
      doingReplySetupDetails boolean added.  When true DialogSetupDetails() is used for WSJT-X reply configuration.  When false it is used for basic WSJT-X configuration.  This boolean
        is saved by onSaveInstanceState() and recalled by updateValuesFromBundle().  It is cleared in dialogNewFeaturesDismiss(), manageDialogSetup(), and dialogSetupAskForDetails()
        since they are only called for basic WSJT-X configuration.  It is also cleared by dialogSetupDetailsNext() when user has stepped through all the instructions and by
        dialogSetupDetailsCancel() when user cancels.  So the idea is to keep this clear except when really needed.  It is only set by MainActivity.dialogReplyHelpSetupListener().
      manageReplySetupDetails() method added.  It is similar to manageDialogSetupDetails() except that it handles WSJT-X Reply configuration.  It is called from dialogSetupDetailsNext(),
        dialogSetupDetailsBack(), and MainActivity.dialogReplyHelpSetupListener().
      dialogSetupDetailsNext() and dialogSetupDetailsBack() was modified to manage both configurations.  Behavior is conditioned on doingReplySetupDetails.  The main change was that it
        calls manageReplySetupDetails() instead of manageDialogSetupDetails() based on doingReplySetupDetails.

1.78-ads-09-19-2019-1347.aab - versionName "1.78", versionCode 25.  Release name 1.78-ads-09-19-2019-1347.  Beta release.  Code is archived as 09_19_2019_1347_WSJTXMonitor.zip.
  There were no changes here, other than the "What's new" string.  This was for testing the mapping update.
1.79-ads-09-19-2019-1426.aab - versionName "1.79", versionCode 26.  Release name 1.79-ads-09-19-2019-1426.  Beta release.  Code is archived as 09_19_2019_1426_WSJTXMonitor.zip.
  The mapping features were added to this build.  That and the "What's new" string are the only changes between v1.79 and v1.78.  Released to beta on 9/20/2019.

1.81-ads-12-07-2019-1707.aab - versionName "1.81", versionCode 27.  Release name 1.81-ads-12-07-2019-1707.  Full release.  Code is archived as 12_07_2019_1707_WSJTXMonitor.zip.
    Primay change was the setting menu.  The settings menu allowed the user to clear the screen from the app or from WSJT-X.  It also allowed the user to display distance in miles
    or kilometers.  Finally it displayed the status of alerts, filters, sorting, my call/grid and DX call/grid.
    Also fixed bug in that MSK-144 was not displaying decodes on the main screen because no msg1 are ever sent.
  Compared against release v1.77.
    Five new DialogFragment classes and one new layout file:
      1) DialogSettings and rect_box_gradient.xml (provides shading and border for settings menu)
      2) DialogStatus
      3) DialogKmMiles
      4) DialogClearScreen
      5) DialogDontShowAgain and dialog_dont_show_again.xml (layout file)
    BaseActivity
      screenAlwaysOnForDisplay was changed from private to protected so that MainActivity could send it as an extra in the intent that starts MapsActivity.
      onPrepareOptionsMenu() - if dataNeverFlowed == true then I renamed the settings menu option to setup WSJT-X title.  This was to allow that option to be found easier, since it
        has not been moved to the Settings submenu.
      onOptionsItemSelected() - if dataNeverFlowed == true then when the second item (action_settings) is selected it will bring up DialogSetup instead of DialogSettings.  This supports
        the change made to onPrepareOptionsMenu().
      dialogSettingsSelectionListener() added.  This calls several settings submenu DialogFragments.
      dialogSettingsHelpListener() added.  This invokes static class DialogSettingsHelp immediately below it.
      DialogSettingsHelp static class added
      dialogKmMilesDoneListener() added.  This switches the units from miles to km.  It will invoke DialogDontShowAgain dialog if the choice has changed.
      dialogClearScreenDoneListener() added.  It tells UDPService to clear the screen when the UDP message is received.  It will invoke DialogDontShowAgain dialog if choice is to enable.
      dialogClearScreenHelpListener() added.  It invokes static class DialogClearScreenHelp immediately below it.
      DialogClearScreenHelp static class added.
    DecodeDataStruct
      In order to support displaying distance in miles I added a String element named distanceInMi
    ReturnStationData
      In order to support displaying distance in miles I added a String element named distanceInMi
    HelperStationList
      In order to support displaying distance in miles I added a String element named distanceInMi to the private class StationData.  That element was filled in the checkStation()
      method when the StationData.distance element is filled in.
    MapsActivity
      Received the screenAlwaysOnForDisplay boolean from MainActivity as an extra in the intent that invokes MapsActivity.  If set then I set the FLAG_KEEP_SCREEN_ON flag.
    MainActivity
      onOptionsItemSelected() - Sent screenAlwaysOnForDisplay as an extra in the intent that starts MapsActivity.
      dialogSortingNextListener() - invoked the UDPService.describeSorting() interface method instead of building the string locally.
    UDPService
      Fixed bug in that MSK-144 was not displaying decodes on the main screen because no msg1 are ever sent.
      Made two one-line methods, doRingtonePre1() and doVibratePre26() in order to suppress the warnings without suppressing any warnings in the surrounding code.
      Added interface methods:
        setKmOrMiles() - simply sets private boolean variable distanceInMiles.
        clearScreen() - forces an immediate clearing of the main display by zeroing displaySpanBufferMain and invoking another local broadcast to MainActivity.
        enableClearScreenFromWSJTX() - sets private boolean clearScreenFromWSJTX, enabling clearing the screen when WSJT-X sends the message.
        describeSorting() - short method that returns a String of the sorting method description.  It is needed by DialogStatus class
        getMyCallAndGrid() - returns string concatenating private String variables myCall and myGrid.  If not known it adds "unknown"
        getDXCallAndGrid() - returns string concatenating private String variables currentDXCall and currentDXGrid, collected from msg1.  Adds "unknown" if unknown.
      udpLoop() - started to add a timeout.  It worked but there is no clean way to shut down app from there.  However, the "catch (SocketTimeoutException ex)" block is left.
      processPacket() - if clear screen UDP msg received and clearScreenFromWSJTX == true then calls clearScreen().
      buildBurstAndAppend() - dashed line is not appended to displaySpanBufferMain if suppressDashedLine is set (for MSK-144).  Also if distanceInMiles set then
        decodeDataArray[iii].distanceInMi is used instead of distanceInKm.  Finally, if interface method clearScreen() was called while this method was running then it
        doesn't attempt to clear screen but instead sets clearScreenWhenDoneWithBurst flag.  At end of this method it checks if the flag is set and zeros out
        displaySpanBufferMain, resulting in the normal screen update mechanisms clearing the screen.
      convertWSPRDecodeMessage() - filled in distanceInMi element returned by call to helperStationList.checkStation().
      convertDecodeMessage() - filled in distanceInMi element returned by call to helperStationList.checkStation().  Also it checks to see if the decode is from MSK-144
        (indicated by the mode field being set to '&').  If so it forces an immediate update of the main display screen by setting endOfBurst (MSK-144 doesn't use msg1).
      convertStatusMessage() - collects DX call and DX grid.
    WSJTXUtils
      Added the following methods:
        getKmMilesDontShowAgain(), setKmMilesDontShowAgain() - sets/gets the dont-show-msg-again flag for displaying km or miles
        getClearScreenDontShowAgain(), setClearScreenDontShowAgain - sets/gets the dont-show-msg-again flag for clearing screen from WSJT-X
        getClearScreenFromWSJTX(), setClearScreenFromWSJTX() - sets/gets the clear screen from WSJT-X flag
        getMilesNotKm(), setMilesNotKm() - sets/gets the miles not km flag.  If set then the distance is displayed in miles.
    main_menu.xml
      removed action_port and action_setup from this file (these two menu options have been moved to settings menu).  Placed action_settings in their place.
    strings.xml
      many changes to suppor the new dialogs, what's new, etc.  One item of note is that the dialog_reply_pre_checkbox string was renamed dialog_dont_show_message_again
        since there are now many dont-show-again dialog invokations.
    DebugUtils (debug and release versions)
      added several constants to support logging on/off of the five new dialogs.

NOTE - on 12/16/2019 I removed the aab files except for 1.81.  I did this just to make the zip files smaller.  I can retrieve the old ones from the archive if necessary.

1.83-ads-12-30-2019-2331.aab - versionName "1.83", versionCode 28.  Release name 1.81-ads-12-30-2019-2331.  Beta release.  Code is archived as 12_30_2019_2331_WSJTXMonitor.zip.
      Primary change was the ability to edit filters and alerts.  The Countries menus can also select and deselect all.  Finally, the Alerts.describeAlerts() and
      Filters.describeFilters() were modified to spit out the first few country names and then say "...+301 more".
      Another important change is that, since it is a beta release, it includes the mapping feature.
  strings.xml for Russian is available.  It has the setup instructions only translated.  I did translate a couple more things than the Chinese.
  strings.xml also added a couple of messages "Select All", "Deselect All" as well as an error message if a filter/alert was unable to read and load for editing.  The corresponding
    help screens were modified to mention Edit Previous Filter/Alert.  In help_previous_filters/alerts I removed the last line about the filter/alert taking effect immediately.  I
    did this so that I could use the same help message for recalling previous filter/alert and for editing them.
  string_array_init.xml (where the filters/alerts text is loaded) was modified to add "Edit Previous Filters" and "Edit Previous Alerts".
  Alerts.java had several methods added to retrieve private variables for editing purposes.  (Called from AlertsInterface.prepareToEditAlert()).  Also, describeAlert() was modified
    to add the "...+301 more" when there are more countries than will fit within 55 characters. MAX_ALERT_DESCRIPTION_LENGTH was expanded from 50 to 55, reason in comments below definition.
  Filters.java was modified similarly to Alerts.java, although the changes are less complex.  New methods added to retrieve private variables (called from
    FiltersInterface.prepareToEditFilter()).  describeFilter() changed to add the "...+301 more" text when there are too many countries.  MAX_FILTER_DESCRIPTION_LENGTH not changed, still 50.
  AlertsInterface.java added prepareToEditAlert() method.  It reads private variables from the Alert object that the user chose to edit and populates the various checkedItems*[] arrays.
    doNext() changed to enter DIALOG_PREVIOUS_ALERTS if user chose previous (checkedItemsNewReuse[1]) or edit (checkedItemsNewReuse[2]) from DIALOG_NEW_OR_REUSE.  If doNext() is
    invoked from DIALOG_PREVIOUS_ALERTS it will do as before if previous (checkedItemsNewReuse[1]) alert.  If edit (checkedItemsNewReuse[2]) it will invoke prepareToEditAlert()
    to setup the Alert object to be edited and then transition to DIALOG_REGION_OR_PREFIX.  Finally, bringUpDialog() was modified.  If DialogName was DIALOG_MAJOR_COUNTRIES or
    DIALOG_ALL_COUNTRIES then the first parameter passed to DialogFilterAlerts is set true, otherwise false.  (See changes in DialogFilterAlerts).
  FiltersInterface.java had almost identical changes as AlertsInterface.java except it operated on Filter objects.
  DialogFilterAlerts.java was modified to allow the insertion of a "Select All" or "Deselect All" option.  An extra parameter, boolean selectAll, is added.  Then the checkedItems[] and
    charSeq[] arrays that are passed are loaded into private checkedItemsUsed[] and charSeqUsed[].  Almost all the changes to accomplish this are in onCreateDialog().  It is heavily
    documented.

1.84-ads-01-12-2020-1156.aab - versionName "1.84", versionCode 29.  Release name 1.84-ads-01-12-2020-1156.  Beta release.  Code is archived as 01_12_2020_1156_WSJTXMonitor.zip.
      Primary change was a bug fix.  Changes are in AlertsInterface.java, FiltersInterface.java, and DialogFilterAlerts.java.  The bug was that the display (but not the function)
        of filters and alerts was not mutually exclusive when the DIALOG_NEW_OR_REUSE was displayed.  This was caused by the doSelection() method of Alerts/FiltersInterface.java
        not clearing the checkedItemsUsed[] element.  It was also necessary to make DialogFilterAlerts.checkedItemsUsed protected instead of private.
      Another change was that I removed mapping, since this is what I intend to release.

1.85-ads-01-21-2020-1057.aab - versionName "1.85", versionCode 30.  Release name 1.85-ads-01-21-2020-1057.  Full release.  Code is archived as 01_21_2020_1057_WSJTXMonitor.zip.
      There was no change between this and 1.84-ads-01-12-2020-1156 other than the versionName and versionCode.

1.86-noads-01-27-2020-1850.aab - versionName "1.86", versionCode 31.  Release name 1.86-noads-01-27-2020-1850.  First Paid release.  Released to Internal Test.  Code is
      archived as 01_27_2020_1850_WSJTXMonitor.zip.

1.88-ads-02-11-2020-2012.aab, 1.88-noads-02-11-2020-2012.aab - versionName "1.88", versionCode 32.  Release name 1.88-ads-02-11-2020-2012, 1.88-noads-02-11-2020-2012.
      Release to internal test.  Code is archived as 02_11_2020_2012_WSJTXMonitor.zip.
  The below comparison is against release 1.85-ads-01-21-2020-1057.  Some of these changes were in 1.86-noads-01-27-2020-1850 paid release to internal test.
    Icons have been split between ads and noads versions.  ic_launcher.png, ic_launcher_foreground.png, and ic_launcher_round.png now have separate versions in their respective
        mipmap-* directories.  Also (for API 26+) values/ic_launcher_background.xml which contains the background color of the icon.  Also, a new hi-res icon was placed in
        src/main/ directory and named ic_launcher-web-pro.png.  Finally, there are new red reply icons (currently unused) src/main/res/drawable-*/ directories, baseline_reply_red_36.png.
        Notes on these separate icons are in Paid Version.docx, first page, near the bottom.
    AndroidManifest.xml has been split in two with the mapping activity moved to src/noads/ directory.  It is then merged with the manifest in src/main/ (see 1/12/2020 notes).
    build.gradle was changed in an attempt to name the app different between debug and release versions.  It didn't work so it is hardcoded in.  However, the app name has "Pro"
        appended to it for the paid version.  Also, the mapping and "set Rx freq" are conditioned on Java variables includeMapping and includeSetRxFreq defined in build.gradle.
        This is described in 01/12/2020 notes (resValue comments).
    MapsActivity.java has also been moved to src/noads/java/... and src/ads/java/... directories, two versions, the noads version to do mapping and the other to satisfy linking.
    WSJTXUtils.java has four methods added, getIsFinishInvoked() and setIsFinishInvoked() to accomodate shutdown, and getIpHashCode() and setIpHashCode() to store the IP
        address.  Note that the IP address isn't stored until comms have been successfully accomplished (in UDPService.processPacket()).
    UDPService.java - SetRxFreq variables have been added (functional in debug version only).
        setInetAddress() interface function has had a bug from the beginning.  The private variable initAddress was never set.  The UDP port still attached to the default IP,
          which was desired.  This bug has been fixed and it will attach to the same IP but it will be explicitly assigned.
        doLocalBroadcastMain() and doLocalBroadcast() were created to make the code neater.  When a local broadcast is done (mostly in udpLoop()) it calls these methods.
        getSetRxCharSeq(), setRxFreqDialogStatus(), setRxFreqSend(), and setRxFreqSendIt() interface functions defined.  Functional only in debug mode.
        processPacket() was modified to store the IP address when the first valid packet arrives.
        buildBurstAndAppend() added a block of code near the bottom for SetRxFreq.  It goes through if the dialog is not up and if not WSPR.  It builds up setRxFreqArray[]
          used in dialog.  Functional only in debug version.
        convertDecodeMessage() changed to pass isMyCall to Alerts.doAlert().  Also, for SetRxFreq the raw bytes needed for msg4 are stored in decodeData (and parsed into
          setRxFreqArray[] in buildBurstAndAppend()).
    DecodeDataStruct.java - modified to add msg4Buffer and msg4Length and a constructor added to create msg4Buffer.  This is to accomodate SetRxFreq feature.
    MainActivity.java
        onCreate() modified to display long message on main screen if ipAddressChanged == true.
        manageDisplay() changed to clear ipAddressChanged when it receives first local broadcast from UDPService.
        onCreateOptionsMenu() changed to add mapping option if includeMapping true and to add red reply icon if includeSetRxFreq true (both booleans defined in build.gradle).
        onPrepareOptionsMenu() changed to replace DisplayAll menu option to setup WSJTX optin if data has never flowed.  This moved it from overriding the Settings menu option
          because if the Settings option was not available then the user could not change the port value.
        onOptionsItemSelected() changed to handle setRxFreq selected.  Also action_display_all selection changed to handle call to DialogSetup if data has never flowed.
        onResume() modified to see if a shutdown has been invoked from DisplayAll activity.  DisplayAll will store boolean in WSJTXUtils.setIsFinishInvoked().  MainActivity.onResume()
          will check this, clear it, and invoke BaseActivity.doFinish() if set.
        DialogSetRxFreq listeners were added.
    HelperStationList.java changed to fix bug (02/10/2020 notes).  setMyGrid() changed to always store private variable myGrid as a 6 character value.  azdist() modified to only
        compare the decoded grid against the first four characters of myGrid (instead of all characters).  Also a bug, present from the start, was fixed.  doFilterForMapping() and
        stationHasBeenWorked() use lastStationUsed private variable without first checking if it is not null.  If the first decoded message has no station (which means there is
        nothing in stationList linked list) then lastStationUsed will be null.
    HelperCountriesList.java had VP8 changed from South Shetland Is to Falkland Is.
    DisplayAll.java - isDisplayAll variable set (defined in BaseActivity and used for shutdown).  The check on dataNeverFlowed was removed because this activity can no longer
        be displayed until first getting data to flow.  onResume() modified to clear initialStartup and to check if a shutdown was initiated.
    DialogSettings.java - the first option was changed from "Clear Screen Now" to shutdown now.  The second option was changed to offer the clear screen submenu.
    DialogClearScreen.java - completely changed from offering the "clear screen from WSJT-X submenu" to offering "clear screen now" and "clear screen from WSJT" options.
    DialogClearScreenWSJTX.java - added to provide the "clear screen from WSJTX" submenu.
    BaseActivity.java
        Listeners for DialogIPChanged, DialogClearScreenWSJTX were added.
        onCreate() - the code for checking if version changed was moved to be within initialStartup block.
        onPrepareOptionsMenu() removed code to use Settings menu option for setup WSJTX since this is accomplished in MainActivity replacing the DisplayAll option with setup WSJTX.
        onOptionsItemSelected() removed similar code for Settings / setup WSJTX switch.
        checkWifi() changed to read IP hash code.  If nonzero and if the current IP doesn't match the stored IP then ipAddressChanged set to true.
        doFinish() method added to accomplish shutdown.  This shuts down the current activity only.  Other activities are shutdown with onResume() checking stored booleans.
        manageDialogSetup() modified to check ipAddressChanged and, if set, invoke DialogIPChanged.
        dialogIPChangedHelp() listener added to invoke help dialog from DialogIPChanged.
        dialogSettingsSelectionListener() changed the first option response to initiate shutdown.  If isDisplayAll set (indicating that it was invoked while DisplayAll activity was
          up) then it will call WSJTXUtils.setIsFinishInvoked() to store variable so MainActivity.onResume() can accomplish shutdown of MainActivity.
        dialogClearScreenSelectionListener() modified to clear screen immediately on selection 0 and invoke DialogClearScreenWSJTX if selection 1.
        dialogClearScreenHelpListener() added.
        DialogMsgOnlyNoTitle static class added.  This removed a couple of other similar static classes.  It is invoked, with the message paramater, from dialogPortHelpClick()
          listener, dialogClearScreenWSJTXHelpListener(), dialogClearScreenHelpListener(), and dialogSettingsHelpListener().
    DialogIPChanged.java has been added to alert the user when the IP changes.
    DialogSetRxFreq.java has been added.  This is only functional in the debug versions.
    AlertsInterface.java - modified to add "alert on my call".  This option was added as the first item in checkedItemsRegionPrefix[] and charSeqRegionPrefix[].  So doCancelHelp(),
        doSelection(), prepareToEditAlert() were modified to change the various indicees.  Most of the functional changes were in Alerts.java.
    Alerts.java -
        alertOnMyCallsign private variable added.
        alertOnAllParam parameter passed to constructor and assigned to alertOnMyCallsign.  Similarly second constructor (used for copyOf()) was modified.
        getAlertOnMyCallsign() interface method added.  Called from AlertsInterface.prepareToEditAlert() when recalling a previous alert and used to fill in checkedItemsRegionPrefix[0].
        copyOf(), describeAlert(), and equals() modified to accomodate alertOnMyCallsign.
        doAlert() modified with an additional parameter, isMyCall.  Invoked from UDPService.convertDecodeMessage().  If alertOnMyCallsign is set and isMyCall parameter set then return true.
    main_menu.xml - comments added for action_reply and action_set_rx placement.
    ids.xml - added actin_set_rx as a new menu item.
    string_array_init.xml - added "Alert on My Callsign" to alert_region_prefix_values.  Spanish added "Alerta en Mi Indicativo" to same.
    strings.xml - dialog_ip_changed_* strings, help_region_prefix_alert added help for alert on my callsign, alert_description_alert_on_my_call added as part of AlertsInterface,
        waiting string changed to reflect that setup WSJT-X is not first (not second) option, enable/disable have been moved to miscellaneous, action_set_rx (set Rx freq) menu item,
        dialog_settings_* menu items changed (dialog_settings_clear_screen_WSJTX removed, dialog_settings_shutdown added, dialog_settings_help changed to mention shutdown),
        dialog_clear_screen_* (used for clear screen submenu), dialog_clear_screen_WSJTX_*).

1.89-noads-02-24-2020-1605.aab - versionName "1.89", versionCode 33.  Release name 1.89-noads-02-24-2020-1605.  Full release.  Code is archived as 02_24_2020_1605_WSJTXMonitor.zip.
    Full release to PAID version ONLY.  The only differences between this and 1.88 is the versionCode, versionName.

1.90-ads-03-01-2020-1832.aab, 1.90-noads-03-01-2020-1832.aab - versionName "1.90", versionCode 34.  Release name 1.90-ads-03-01-2020-1832, 1.90-noads-03-01-2020-1832.
        Full release for ads version, internal test release for noads version.  Code is archived as 03_01_2020_1832_WSJTXMonitor.zip.
    This was supposed to be the same as v1.88 and v1.89.  However, in order to give the ads version a link to the noads version I had to move two strings out of strings.xml and into
        separate strings.xml files for ads and noads.  In addition I found a bug in the Android library (InetAddress.hashCode()).  These issues are detailed in 2/27/2020 notes.
      BaseActivity.java - protected variable ipAddressInt added.  In checkWifi() it is used to store the correct IP hash code, since it needs to be computed anyhow.  The call
        to InetAddress.hashCode() was remvoed.  In ServiceConnection() it is passed to UDPService.setInetAddress().
      UDPService.java - private variable ipAddressHashCode added.  In setInetAddress() it is assigned the hash code created in BaseActivity.checkWifi().  In processPacket(), when
        data begins to flow, it is passed to WSJTXUtils.setIpHashCode().  This removed the need to call InetAddress.hashCode().
      strings.xml - about_notes and new_features_text commented out (moved to noads/values/strings.xml and ads/values/strings.xml.  Same for values-zh/strings.xml and
        values-ru/strings.xml.  Also, dialog_ip_changed_title was shortened.
      values-es/strings.xml - exact same changes as strings.xml.  The new files are noads/values-es/strings.xml and ads/values-es/strings.xml.

1.91-ads-03-24-2020-2225.aab, 1.91-noads-03-24-2020-2225.aab - versionName "1.91", versionCode 35.  Release name 1.91-ads-03-24-2020-2225, 1.91-noads-03-24-2020-2225.aab
        Full release for both versions.  Code is archived as 03_24_2020_2225_WSJTXMonitor.zip.
  The only change here was the addition of a dont-show-again Dialog that warned the user that the free and paid versions cannot both be run at the same time because they access
      the same network port.  An additional webpage was set up which gave details on how to change network ports to allow them both to run at the same time.  If the dialog pops up
      it occurs after the DislogSetupDetails (4 page setup instructions) and in place of DialogSetup (at startup only).  See 3/22/2020 notes.
    BaseActivity.java - isOppositeAppInstalled() added.  It checks to see if both apps are installed (getting the string to search for from strings.xml).  If so
      then it brings up the DialogDontShowAgain dialog.  It returns true of dialog brought up, false otherwise.  Method manageDialogSetup() was modified to call
      isOppositeAppInstalled() if dataNeverFlowed == false and ipAddressChanged == false.  This is how the new dialog replaces DialogSetup.  dialogSetupDetailsNext()
      was modified to call isOppositeAppInstalled() after the four page setup is complete (DialogSetupDetails).
    DialogDontShowAgain.java - onCreateDialog() was modified with a call to TextView.setMovementMethod() to allow for a link to be added to dialog.  The listener for the
      checkbox (setOnCheckedChangeListener()) was modified to add the call to WSJTXUtils.setBothAppsDontShowAgain() if the user clicked the checkbox.
    WSJTXUtils.java - methods getBothAppsDontShowAgain() and setBothAppsDontShowAgain() added to remember the dont-show-again checkbox settings.
    strings.xml, valies-es/strings.xml, valies-ru/strings.xml, valies-zh/strings.xml - dialog_both_apps_title and dialog_both_apps_text added for the new dialog.
    ads/res/values/strings.xml, ads/res/values-es/strings.xml - new_features_text modified to reflect new features, including link to paid app.  opposite_app_name to hold
      name of app for isOppositeAppInstalled() to search for.
    noads/res/values/strings.xml, noads/res/values-es/strings.xml - new_features_text modified to reflect new features, no link to paid app.  opposite_app_name added
      to hold name of app for isOppositeAppInstalled() to search for.

2.018-noads-04-07-2020-1457 - versionName "2.018", versionCode 36.  Release name 2.018-noads-04-07-2020-1457.  Full release for paid version.  Code is archived as 04_07_2020_1457_WSJTXMonitor.zip
  The primary change was the "set-rx-freq' feature.  It also included more translations for Russian and Chinese as well as adding translations for Japanese and Portuguese.
    build.gradle - Besides versionCode/versionName changing the includeSetRxFreq variable has been removed.
    dialog_set_rx_freq.xml and simple_list_item_1.xml were added to layout/ directory.  This allowed me to add a custom ListView for the dialog.
    dialog_main_display_help.xml was added.  I added a custom layout in order to display the red arrow.
    dialog_new_featurs.xml was added.  It was added so I could display the red arrow.
    reply_setup_step3.png and reply_setup_step3_sm.png - small change to this graphic changed the port back to the default.
    DialogSetRxFreqHelp.java added.  This is the help dialog for DialogSetRxFreq.  It is passed an integer which determines which form of the help message to display (WSPR mode, no decodes,
        no response from WSJT-X, introduction message, and normal help).
    DialogNewFeatures.java was modified to use a custom layout (dialog_new_features.xml).
    DialogSetRxFreq.java was modified to use a custom ListView layout (dialog_set_rx_freq.xml which, in turn, uses simple_list_item_1.xml for its TextView).
    WSJTXUtils.java had a new method added, getAndBumpNumberOfSetRxFreq().  This counts the number of times DialogSetRxFreq was displayed.  It's only there to indicate if the dialog had
        never been displayed before.  If it hasn't then a special ("introduction") DialogSetRxFreqHelp message will be displayed.
    MainActivity.java -
        - Listener for DialogSetRxFreqHelp added.
        - manageDisplay() has a block added at the end which checks boolean messageSetRxFreqSet.  This boolean is set when user makes a choice to set the rx freq.  It is cleared here
          and then UDPService.getSetRxStatus() is called to see if message to WSJT-X failed and, if so, bring up the "no response from WSJT-X" version of DialogSetRxFreqHelp.
        - includeSetRxFreq boolean, defined in build.gradle, has been removed.
        - onOptionsItemSelected(), case R.id.action_set_rx is not a bit complicated.  If data flowing and current mode is WSPR it brings up "WSPR mode" version of DialogSetRxFreqHelp.
          Next it checks if this is the first time.  If so and data is flowing then it brings up "introduction" version of DialogSetRxFreqHelp.  If data is not flowing then it brings
          up the "no decodes" version of DialogSetRxFreqHelp.  If it is not the first time then, if no decodes, it brings up the "no decodes" version of DialogSetRxFreqHelp.  Otherwise
          it brings up the DialogSetRxFreq dialog.
        - dialogSetRxFreqSelectionListener() modified to set messageSetRxFreqSet boolean true.
        - dialogSetRxFreqHelpListener() modified to bring up normal version of DialogSetRxFreqHelp.
        - dialogSetRxFreqHelpDismissListener() (listener for DialogSetRxFreqHelp Dismiss button) added.  If it was the first time the dialog was attempted then it brings up DialogSetRxFreq.
        - dialogSetRxFreqHelpSetupListener() (listener for DialogSetRxFreqHelp Setup button) added.  It invokes the three page setup instructions for allowing WSJT-X to respond to messages.
        - MainDisplayHelp static class modifed to use a custom layout (dialog_main_display_help.xml) in order to allow the red arrow to be displayed.
    UDPService.java -
        - SetRxFreqData static class expanded to add String element callsign and int dFreq, both used in setRxFreqSend() interface function to see if user is selecting the same station as
          new selected.  Also added private constructor and public copyOf() method.
        - Added some additional variables, most significantly a second SetRxFreqData[] array.  The first, setRxFreqArray[], is used as before, within buildBurstAndApend().  The second,
          SetRxFreqDataCopy[] is used to hold the data displayed by DialogSetRxFreq.  If bursts go by while this dialog is up then if the dialog is dismissed and brought up again it will
          refresh SetRxFreqDataCopy[] with a new copy.
        - timerRunnable was added.  The run() method handles the timeout feature.
        - getSetRxStatus() interface function added.  Returns status of message (-1 failure, 0 unknown, +1 success).  Called from MainActivity.manageDisplay().
        - isCurrentModeWSPR() interface function added.  Returns true if last msg1 indicates WSPR.
        - getSetRxCharSeq() interface modified.  It first checks if setRxFreqArray[] is being modified (setRxFreqArrayInUse is true).  If clear and there is data in setRxFreqArray[] then
          it copies data from setRxFreqArray[] to setRxFreqArrayCopy[].
        - setRxFreqSend() interface method modified.  This method initiates a message being sent to WSJT-X.  It first checks to see if the current DXCall and DXFreq are the same as what
          the user selected and, if so, does nothing.  Otherwise, before it starts the thread to send message, it stores current selection, current DX call, and current DX freq for use
          in comparison within timerRunnable.  It then starts the timer.
        - setRxFreqSendIt() - the method launched by thread2.run() (initiates message to WSJT-X).  The only change was that the selection is pulled from setRxFreqArrayCopy[] instead of setRxFreqArray[].
        - buildBurstAndAppend() - the loading up of data for setRxFreqArray[] array was moved from a separate loop (which was removed) into the main loop.  As the strings that make up the
          main display data are build then the elements of setRxFreqArray[] are loaded up at the same time.
        - convertStatusMessage() modified to set/clear currentModeWSPR and load DX freq into currentRxFreq.
    noads/ res/ values/ strings.xml and ...values-es/ strings.xml reflect new features
    res/ values/ strings.xml and ..values-es/ strings.xml - main display help lines modifed slightly, main_display_text9 broken in two, one in same place as before, the other part is
        after the message about the red arrow.  The red arrow text was added (main_display_text10).  reply_header_text_step3 (the last page of the graphical display on how to set WSJT-X
        to receive messages) did not, until now, show "Image scrolls up/down, left/right".  Finally, additional strings were added at the end of the file to support DialogSetRxFreqHelp.
    res/ values-ja/ strings.xml added.  This has the Japanese translation of setup messages.
    res/ values-pt/ strings.xml and ..values-pt/ styles.xml added.  This has the Portuguese translation of setup message.  The styles was necessary because the text in the buttons for
        Spanish and Portuguese take more room, so that the text must be slightly smaller.

2.023-noads-04-24-2020-1241 - versionName "2.023", versionCode 37.  Release name 2.023-noads-04-24-2020-1241.  Paid version release to beta.  Code is archived as 04_24_2020_1241_WSJTXMonitor.zip.
  The primary change was the addition of Firebase.  A small additional change was that I display the current mode and frequency on the dotted line between bursts and in the Settings/ Current Status
  dialog.  The purpose of this release was to let my tablet update and gain a small amount of experience with this.
    google-services.json - added to app directory.  Currently this config file has info for noads and noads debug versions.  Obviously the free version will change this file.
    build.gradle - Project level.  A dependency was added for Firebase (com.google.gms:google-services:4.3.3)
    build.gradle - app level.  A plug-in for Firebase (com.google.gms.google-services) was added at top of file.  One depencency for Firebase Analytics was added at bottom of file
        (com.google.firebase:firebase-analytics:17.3.0).  Another dependency was added for testing (androidx.test:rules:1.3.0-alpha05).
    res/ values/ strings.xml and all language versions - added one line for Settings/ Current Status dialog which says "Freq/Mode" or "Frec/Modo".
    WSJTXUtils.java - modified getAndUpdateNumberOfPackets() not bother updating the count if the number of new packets is zero.  Added functions to bump and retrieve number of SetRxFreq,
        Mappings, Sortings, and Reples.  Also added function to retrieve the number of filter and alert uses.  Functions added are getAndBumpNumberOfSetRxFreqSelections() and
        getNumberOfSetRxFreqSelections(), getAndBumpNumberOfMappings() and getNumberOfMappings(), getAndBumpNumberOfSortings() and getNumberOfSortings(), getAndBumpNumberOfReplies()
        and getNumberOfReplies(), plus getNumberOfAlertUses() and getNumberOfFilterUses().
    UDPService.java - collected the user's frequency and mode from msg1.  Added getWsjtxFreq() and getWsjtxMode() interface functions to retrieve these values (as Strings).
        convertStatusMessage() was modified to collect these values.  buildBurstAndAppend() was modified to print these value on the dotted lines.
    MainActivity.java - onCreateOptionsMenu() added a FirebaseAnalytics for REPLIES.  onOptionsItemSelected, AlertsInterface and FIltersInterface constructers got passed
        mFirebaseAnalytics parameter, mapping option sends FirebaseAnalytics for MAPPING.  onResumeFragments() modified to call FiltersInterface.setFirebaseAnalytics() and
        AlertsInterface.setFirebaseAnalytics() to pass FirebaseAnalytics instance.  dialogSetRxFreqSelectionListener() (called when user makes a selection) added FirebaseAnalytics
        for RxFreq selection.  dialogSetRxFreqHelpSetupListener() and dialogReplyHelpSetupListener() call BaseActivity.analyticsReportSetup() which does FirebaseAnalytics when user
        chooses to do the WSJT-X setup for replies.  dialogSortingNextListener() added FirebaseAnalytics for SORTING.
    BaseActivity.java - FirebaseAnalytics instance added, mFirebaseAnalytics, initialized in onCreate().  Also a class with the Event names was added, analyticsCustomEvents.  This represents
        all the evens collected except for the predefined event TUTORIAL_BEGIN/COMPLETE.  onCreate() also modified the initial startup section to send FirebaseAnalytics event, INITIAL_STARTUP,
        with all the collected data.  analyticsReportSetup() added.  This handles the multiple places where the WSJT-X setup is used.  It sends a FirbaseAnalytics event TUTORIAL_BEGIN/COMPLETE.
        manageDialogSetup(), dialogIPChangedHelp(), dialogSetupAskForDetails(), dialogSetupDetailsNext(), and dialogSetupDetailsCancel() modified to call analyticsReportSetup().
    AlertsInterface.java - added its own copy of FirebaseAnalytics object, thisFirebaseAnalytics.  The constructor added the FirbaseAnalytics object parameter and initialized it.
        setFirebaseAnalytics() added to set thisFirebaseAnalytics object when screen is rotated (called from MainActivity.onResumeFragments()).  doNext() code was cleaned up.  The
        two exit points where a new Alert was created had duplicate code.  I moved that to finishWithNewAlert() function.  Added analyticsAlerts() function to send FirebaseAnalytics for ALERTS.
    Alerts.java - describeAlertForFirebase() function added which describes the alert in a bit map which is one of the parameters passed to FirebaseAnalytics event ALERTS.
    FiltersInterface.java - modified in a manner identical to AlertsInterface.java.  thisFirebaseAnalytics object added, parameter added to constructor, setFirebaseAnalytics() added,
        finishWithNewFilter() added to eliminate duplicate code and called from doNext(), analyticsFilters() added to send FirebaseAnalytics event FILTERS.
    Filters.java - describeFilterForFirebase() added.  It describes filter in a bitmap which is one of the parameters for FirebaseAnalytics event FILTERS.
    DialogStatus.java - modified to display current frequency and mode.

2.032-ads-05-05-2020-1400 - versionName "2.032", versionCode 38.  Release name 2.032-ads-05-05-2020-1400.  Free version release to internal test.  Code is archived as 05_05_2020_1400_WSJTXMonitor.zip.
  The primary change was the set-rx-freq and the addition of Firebase.  Also, I display the current mode and frequency on the dotted line between bursts and in the Settings/ Current Status
  dialog.  I also highlight the currently selected DX station in red text.  I also display Mapping on the menus, bring up a bogus map and tell the user that it's only available in paid version.
    ads/java/MapsActivity.java was changed from a stub to one that displayed an artificial map full screen and then brings up a dialog to tell the user that it is only in the paid version.
    ads/res/drawable have two new items, map_italy.png and map_la.png.  This is displayed full screen depending on portrait or landscape mode.
    ads/res/layout/activity_maps.xml was added to display the fake map full screen.
    ads/res/values/strings.xml was modified to display what's new.  Also a string message added for MapsActivity to tell the user that this is only available in the paid version.
    ads/res/values-es/strings.xml same.
    BaseActivity.java - the onCreate() method has a block for initial startup.  That block is now entered in initial startup only for the parent of MainActivity, not DiaplayALl.  This was
        for Google Analytics, so that it doesn't send an initial startup message when DiaplayAll is viewed.
    MainActivity.java
        - manageDisplay() fixed a small bug in set-rx-freq.  Before messageSetRxFreqSet boolean (set when the user makes a selection) was cleared automatically.  Now it calls
          mService.getSetRxStatus() and only cleared when the set-rx-freq message either succeeded or failed.
        - onCreateOptionsMenu() changed to always display the mapping option.  Previously it was conditioned on the version being noads.
        - onOptionsItemSelected(), case R.id.action_mapping, if includeMapping boolean (set in build.gradle) is false then it starts MapsActivity.  Previously it did nothing because the
          menu option wasn't displayed.
        - MainDisplayHelp static class, onCreateDialog() method, the text was changed to add a line about the current DX call being highlighted in red.
    UDPService.java - buildBurstAndAppend() modified to highlight the current DX callsign in red.
        - Also fixed bug - where I was creating the string array for set-rx-freq dialog I was also
          adding a line for alertArray.  This caused the notification message to show the station causing the alert twice.  I eliminated that line.
        - Fixed another bug - currentDXCall is initially null.  Testing with HSMS, where msg1 is not normally used, left currenDXCall null.  So every place it is compared needs to first check
          that it is not null.  timerRunnable checks currentDXCall (and currentRxFreq) to see they are assigned a value.  setRxFreqSend() was modified to check these two variable and, if not
          assigned it won't initiate a msg4 sent to WSJT-X (which should make the changes to timerRunnable unnecessary).  Finally, in buildBurstAndAppend(), where it checks to see if the
          text should be in red, it first checks to see that currentDXCall != null before comparing against the callsign in the burst.
        - A similar bug occurs with wsjtxFreq and wsjtxMOde, which are initialized to null.  Interface methods getWsjtxFreq() and getWsjtxMode() now check for null and, if so, returns an empty
          string.
    DialogStatus.java - UDPService.getWsjtxFreq() and UDPService.getWsjtxMode() are checked to make sure it is not an empty string before printing the last line (freq/mode line)
    res/values/colors.xml - added dxCallColor.
    res/values/strings.xml - added lines to Main Display Help section to mention the red text indicating the current DX callsign.
    res/values-es ... values-ja ... values-pt ... values-ru ... values-zh/strings.xml - ditto
    AndroidManifest.xml - the MapsActivity was added.  Previously it was only in the noads/AndroidManifest.xml supplement for the paid app.
    google-services.json - client_info blocks were added for ads and ads.debug versions.
    (noads/res/layout/activity_maps.xml - this file was relocated from main files.  Also noads/AndroidManifest.xml had the MapsActivity removed.)

2.035-ads-05-09-2020-1738 - versionName "2.035", versionCode 39.  Release name 2.035-ads-05-09-2020-1738.  Free version release.  Code is archived as 05_09_2020_1738_WSJTXMonitor.zip.
  The only change here were things involved in allowing the user to turn on and off data collection.  I use a FirebaseAnalytics method call to turn on and off.  However, because I can't
  read the current status I also use a SharedPreference.  Some comments in WSJTXUtils.java and BaseActivity.onCreate() explain more.  Every FirebaseAnalytics.logEvent() is conditioned on
  the SharedPreference functions.   FirebaseAnalytics is only invoked at startup (after reading SharedPreference) and when the user changes the setting.
    build.gradle - only the versionName and versionCode
    AndroidMenifest.xml - four meta-data elements are added.  The first firebase_analytics_collection_enabled is set to false.  It is what is turned on and off with the FirebaseAnalytics
        setAnalyticsCollectionEnabled() method.  The remaining three are thee to restrict what is collected as much as possible.
    AlertsInterface.java - the call to FirebaseAnalytics.logEvent() is conditioned on WSJTXUtils.getDataCollection() returning true.
    FiltersInterface.java - the call to FirebaseAnalytics.logEvent() is conditioned on WSJTXUtils.getDataCollection() returning true.
    MainActivity.java - the call to FirebaseAnalytics.logEvent() is conditioned on WSJTXUtils.getDataCollection() returning true.
    DialogNewFeatures.java - since I'm using a custom layout I have to call setMovementMethod() on the textView that has a link, done in onCreateDialog() for dialog_new_features_text2.
        Also removed onStart() since it called setMovementMethod() for the TextView in the built-in layout.
    WSJTXUtils.java - added getDataCollection() and setDataCollection(), the two SharedPreference functions to handle turning on and off data collections.
    BaseActivity.java - in onCreate() conditioned call to FirebaseAnalytics.logEvent() on WSJTXUtils.getDataCollection() return value.  Passed the same value to FirebaseAnalytics
          setAnalyticsCollectionEnabled() method.
        - analyticsReportSetup() - the call to FirebaseAnalytics.logEvent() is conditioned on WSJTXUtils.getDataCollection() returning true.
        - dialogEULAOkClick() - When user selects OK button from DialogEULA it sets FirebaseAnalytics.setAnalyticsCollectionEnabled() to the value returned by WSJTXUtils.getDataCollection().
    DialogEULA.java - static class DialogEULAOptInOut added.  onCreateDialog() changed: Negative button added, set to OPT-OUT or OPT-IN.  onResume() added to prevent Dialog from closing
        when user presses OPT-OUT/IN button.  onResume() first toggles WSJTXUtils.getDataCollection() return value and rewrites it.  It also changes the button text.  Finally it opens up
        DialogEULAOptInOut, a confirmation dialog to acknowledge the user's selection.
    values/eula_strings.xml and values-es/eula_strings.xml - Text added to bottom of EULA string to inform user of his option to turn off data collection.  Also added button text (2) and
        confirmation message text (2).
    ads/values/strings.xml and ads/values-es/strings.xml - added a link to the privacy policy on the About dialog text.  Added mention and a link to the privacy policy on new features text.

2.036-noads-05-10-2020-2107 - versionName "2.036", versionCode 40.  Release name /2.036-noads-05-10-2020-2107.  Paid version release.  Code is archived as 05_10_2020_2107_WSJTXMonitor.zip.
  This release was to get the paid version caught up on a couple new features that I added in v2.035 free version.  The feature of putting the frequency and mode on the dotted line,
  the feature of printing the DX station in red, Google Analytics and the ability to opt-out.
    DialogEULA.java, AndroidManifest.xml - added or corrected comments.
    build.gradle - updated versionCode and versionName
    noads/res/values/strings.xml, noads/res/values-es/strings.xml - Added privacy policy link to string about_notes.  Modified new_features_text string to reflect new features, including
        a note on the privacy policy changing.  Removed new_features_text2 from Spanish and blanked it out on English.  It had to be there to satisfy the linker since dialog_new_features.xml
        was still in the source code, even though it is not used.
    UDPService.java - the call to getResources().getColor() was deprecated.  Replaced with ContextCompat.getColor().
    MainActivity.java - the call to getResources().getColor() was deprecated.  Replaced with ContextCompat.getColor().
    DialogNewFeatures.java - removed the code that handled the custom layout, dialog_new_features.xml, and reinstalled the code that used the default layout.

2.048-noads-06-01-2020-1549, 2.048-ads-06-01-2020-1549 - versionName "2.048", versionCode 41.  Release name 2.048-noads-06-01-2020-1549, 2.048-ads-06-01-2020-1549.
  Full release for both versions.  Code is archived as 06_01_2020_1549_WSJTXMonitor.tgz.  THIS CODE WAS UPLOADED BUT NOT ACTUALLY RELEASED.
  There are two bugs fixed here.  First, the DX Call, if heard, is highlighted in red in the Select Station window, in addition to the main window.  The second bug was that
  the Select Station feature did not work if the DX Call field is blank.  This was especially a problem in JTDX, which always starts up with those fields blank.
    noads\res\values\strings.xml, noads\res\values-es\strings.xml, ads\res\values\strings.xml, ads\res\values-es\strings.xml - changed what's new lines.
    build.gradle - updated versionCode to 41 and versionName to 2.048
    DialogSettings.java - fixed a few Java warnings about possible null values.
    UDPService.java - this is where the bugs are fixed.  In setRxFreqSend() the first IF block was intended to reject the message if WSJT-X msg1 had not arrived.  I conditioned that
        on three things, one of which was whether currentDXCall was null or blank.  This was removed and it is now conditioned only on currentRxFreq being zero.  msg1 will always set this.
        Also, in buildBurstAndAppend() I noted if DX Call is in the current burst list.  I was already highlighting that in red.  But I also set a boolean variable, isMyCallSoMakeTextRed
        (which should be renamed isDXCallSoMakeTextRed).  Then, further down, when building setRxFreqArray[] for Select Station window, I check this variable and highlight the text of the
        current line in red if it is set.

2.052-noads-06-02-2020-1723, 2.052-ads-06-02-2020-1723 - versionName "2.052", versionCode 42.  Release name 2.052-noads-06-02-2020-1723, 2.052-ads-06-02-2020-1723.
  Full release for both versions.  Code is archived as 06_02_2020_1723_WSJTXMonitor.tgz.
  I had uploaded the previous version (v2.048, vc41) but WSJT-X released v2.2.0 which did three decoding passes for FT8.  This meant three sets of status messages (msg1).  So I modified
  UDPServer to count the number of decodes before setting endOfBurst variable, which allows buildBurstAndAppend() to be called.
    noads\res\values\strings.xml, noads\res\values-es\strings.xml, ads\res\values\strings.xml, ads\res\values-es\strings.xml - Added a third line to v2.048 changes, mentioning v2.2.0 WSJT-X.
    build.gradle - updated versionCode to 42 and versionName to 2.052.
    UDPService.java - added decodeCounter (int, initialized to 0), threePassDecode (boolean, initialized true), and timeSinceLastDecode (long, to 0).
        In convertHeartbeatMessage() I read the version and if 2.2.0 or greater then set threePassDecode true.  Actually, just checked that the first and third digit are both 2 or greater.
        In convertStatusMessage() I first check the mode.  If FT8 and threePassDecode true then I set local boolean threePassDecodeInUse true.  Further down, where decode boolean is checked
          to see if it transitioned from true to false I check threePassDecodeInUse.  If false then it sets endOfBurst as before.  If true it bumps decodeCounter and if it is 3 or greater
          then it clears decodeCounter and sets endOfBurst.  Before doing any of that it checks the current time and compares it against the last decode true-false transition.  If more than
          7 seconds have elapsed then it sets decodeCounter to zero.  This is a safety check in case one of those status messages is lost.

2.058-noads-07-14-2020-2037 - versionName "2.058", versionCode 43.  Release name 2.058-noads-07-14-2020-2037.  Code is archived as 07_14_2020_2037_WSJTXMonitor.tgz.
  Release to internal testing, paid only.  There are two changes.  One was simple bug fix in UDPServer.java on catching WSJT-X version.  The other was that in FiltersInterface and AlertsInterface I
  made sure Major Countries list is always in synce with All Countries list.
    build.gradle (project) - update to gradle 4.0.0
    build.gradle (app) - compileSdkVersion and targetSdkVersion bumped to 29, updated several libraries and added androidx.preference:preference:1.1.1 (because PreferenceManager updated to
        androidx in WSJTXUtils.java.
    activity_maps.xml - rearranged lines to try to prevent strange error r.id.maps not recognized (line 90 MapsActivity.java).  It doesn't seem to cause harm.
    MapsActivity.java - removed unused import statements.
    WSJTXUtils.java - PreferenceManager import statement updated to androidx.  THis was the cause of androidx.preference:preference:1.1.1 being added to build.gradle.
    UDPService.java - in convertHeartbeatMessage() the WSJT-X version is extracted and if > 2.2.0 then threePassDecode set true.  Previous version had a bug in that if WSJT-X ever moved
        to 3.0.x or 3.1.x it would not set threePassDecode to true.
    FiltersInterface - keep major countries list and all countries list in sync
      - (unrelated) Replaced several FOR loops with Arrays.fill() call.  The FOR loops simply initialized arrays.
      - Added method syncTheTwoCountryLists().  This is what keeps the two lists in sync while the user is creating or editing a filter.
      - prepareToEditFilter() modified.  This is what syncs up the two lists when the user recalls an existing filter.  Previously it had an unexpected behavior in that if a filter had
        only a major country then it would not add it to all countries list.  It would only do that if it also had a minor country in the filter.
      - In doNext() call syncTheTwoCountryLists() unless DIALOG_INCLUDE_EXCLUDE.  The state isn't important because the first thing syncTheTwoCountryLists() does is check to see if
        state is DIALOG_MAJOR_COUNTRIES or DIALOG_ALL_COUNTRIES.  Anyhow, this syncs up the lists if the user presses OK in one of these two states.
      - In doCancelHelp() called syncTheTwoCountryLists() when leaving DIALOG_MAJOR_COUNTRIES or DIALOG_ALL_COUNTRIES state.  This syncs up the lists if the Clear-Return button or back
        arrow is pressed.
    AlertsInterface - exact same changes as in FiltersInterface.
      - (unrelated) Replaced several FOR loops with Arrays.fill() call.  The FOR loops simply initialized arrays.
      - Added method syncTheTwoCountryLists().  This is what keeps the two lists in sync while the user is creating or editing an alert.
      - prepareToEditAlert() modified.  This is what syncs up the two lists when the user recalls an existing alert.  Previously it had an unexpected behavior in that if an alert had
        only a major country then it would not add it to all countries list.  It would only do that if it also had a minor country in the alert.
      - In doNext() call syncTheTwoCountryLists() unless DIALOG_INCLUDE_EXCLUDE.  The state isn't important because the first thing syncTheTwoCountryLists() does is check to see if
        state is DIALOG_MAJOR_COUNTRIES or DIALOG_ALL_COUNTRIES.  Anyhow, this syncs up the lists if the user presses OK in one of these two states.
      - In doCancelHelp() called syncTheTwoCountryLists() when leaving DIALOG_MAJOR_COUNTRIES or DIALOG_ALL_COUNTRIES state.  This syncs up the lists if the Clear-Return button or back
        arrow is pressed.
    noads/values/strings.xml - new_features_text modified.

2.062-noads-07-17-2020-0611 - versionName "2.062", versionCode 44.  Release name 2.062-noads-07-17-2020-0611.  Code is archived as 07_17_2020_0611_WSJTXMonitor.tgz
  Release to internal testing, paid only.  The major change here was that in FT8 mode three-pass-decode I updated the screen with each pass.  Besides making the screen more responsive
  it also helped with users who drop a lot of UDP packets.  All changes were in UDPService.java.
    build.gradle (app) - bumped versionName and versionCode
    UDPService.java
      - Removed setRxFreqArray[] and replaced it with setRxFreqArrayList<>.  This was because, with several passes, I needed to dynamically add to the array in buildBurstAndAppend().
        An array in Java is a fixed size so I needed to change it to an ArrayList<>.
      - For an attempt to estimate packet loss I created two variable, firstHeartbeatMsgTime and heartbeatMessageCounter.  Both are used in convertHeartbeatMessage().  The second simply
        counts heartbeat messages (msg0).  The first records the time of the first heartbeat message.  Since msg0 occurs every 15 sec then I report in Display All Messages window, with
        each heartbeat message, the number of messages and the time since first message.  For this version only I also do the math and report the estimated packet loss rate.
      - getSetRxCharSeq() returns an array of CharSequence for the Dialog display.  It also creates and populates the setRxFreqArrayCopy[] array for use when sending msg4 data to WSJT-X.
        This method was modified to read from setRxFreqArrayList<> instead of setRxFreqArray[].
      - convertStatusMessage() was modified to make this all work.  endOfBurst was set true with each transition from decoding True to False, instead of waiting for all three decodes.
        Then, as decodeCounter counts the three decodes it sets suppressDashedLine True for the second and third decode, setting it False on the first.  This is what signals buildBurstAndAppend()
        to place the dashed line and also to initialize setRxFreqArrayList<>.
      - buildBurstAndAppend() initialized setRxFreqArrayList<> if suppressDashedLine was clear.  Previously setRxFreqArray[] was initialized with every pass.  Further down, the entire
        set-rx-freq data collection was conditioned on setRxFreqArrayList<> not being null (probably should have been all along).  Finally, at the end the data collected in a SetRxFreqData
        object was added to setRxFreqArrayList<> instead of setRxFreqArray[] array.

2.067-noads-07-28-2020-1145 - versionName "2.067", versionCode 45.  Release name 2.067-noads-07-28-2020-1145.  Code is archived as 07_28_2020_1145_WSJTXMonitor.tgz
  The big change here is multicast, enabled and disabled in build.gradle (app) with the multicastIP variable.  In addition, because three pass decoding would sort on each of the three passes
  a dont-show-again dialog is added when user selects a sorting method.
    build.gradle (app) - bumped versionName and versionCode, added multicastIP variable to enable or disable multicast.
    AndroidManifest.xml - CHANGE_WIFI_STATE and CHANGE_WIFI_MULTICAST_STATE added.
    src\main\res\values\strings.xml, src\main\res\values-es\strings.xml - dialog_sorting_dont_text added, dont-show-again message for sorting, warning users of sorting with three pass decoding
    DialogDontShowAgain.java - additional "else if" block, calling WSJTXUtils.setSortingDontShowAgain() if user clicks on the checkbox for sorting dont-show-again dialog.
    BaseActivity.java - checkWifi() method modified to acquire multicast lock if multicastIP true.
    WSJTXUtils.java - functions getSortingDontShowAgain() and setSortingDontShowAgain() added to support sorting dont-show-again dialog.
    MainActivity.java - dialogSortingNextListener() modified to call dont-show-again dialog after user selects a sorting method, conditioned on mService.threePassDecoding() returning true.
    UDPService.java
      - threePassDecodeInUse, previously a local variable in convertStatusMessage(), made into global variable so UDPService.threePassDecoding() can report its status.
      - added threePassDecoding() method to report status of three pass decoding.
      - udpLoop() modified (outside of loop) to initialize a multicast socket if multicastIP true.
      - eliminated several blocks of commented out code.

2.069-noads-08-09-2020-2019, 2.069-ads-08-09-2020-2019 - versionName "2.069", versionCode 46.  Release name 2.069-noads-08-09-2020-2019, 2.069-ads-08-09-2020-2019.  Code
      is archived as 08_09_2020_2019_WSJTXMonitor.tgz.
  This code is identical to 2.067 except that multicastIP is false in build.gradle and the multicast permissions are commented out in the manifest file.
    build.gradle - multicastIP now false, bumped versionName and versionCode
    AndroidManifest.xml - removed the two permissions that are used for multicast.
    ads\res\values\strings.xml - updated free version of Whats-New.  Paid version had been already updated for internal testing.
    noads\res\values-es\strings.xml, ads\res\values-es\strings.xml - updated Spanish version of Whats-New.
    main\res\values-zh\strings.xml, main\res\values-ru\strings.xml, main\res\values-pt\strings.xml, main\res\values-ja\strings.xml - added dialog_sorting_dont_text string (sorting warning
      about three-pass-decoding).  The text is in English for Russian, Chinese, Portuguese, and Japanese.

2.070-noads-03-03-2021-1427 - versionName "2.070", versionCode 47.  Release name 2.070-noads-03-03-2021-1427.  Code is archived as 03-03-2021-1427_WSJTXMonitor.tgz.
  The only change here was that it was compiled for Android 11 (compileSdkVersion == targetSdkVersion == 30).  It was released to Internal Test for Torsten, DL9GTB, who had
  the app crash upon startup after Samsung upgraded to Android 11.

2.071-noads-03-07-2021-1939 - versionName "2.071", versionCode 48.  Release name 2.071-noads-03-07-2021-1939.  Code is archived as 03-07-2021-1939_WSJTXMonitor.tgz.
  The only change was that I prevented the UDPService thread from starting.  Specifically, in UDPService.onStartCommand() I removed the lines threadMethod() and threadRunning = true.

2.073-noads-03-13-2021-1740 - versionName "2.073", versionCode 49.  Release name 2.073-noads-03-13-2021-1740.  Code is archived as 03_13_2021_1740_WSJTXMonitor.tgz.
  This was released to Internal Test.  Complete notes on the problem and the solution is dated 3/12-13/2021.
  UDPService.java
    - in onCreate() the code to set notificationUri is placed in a try / catch block.  Without this the app will barf on startup if the default notification sound is not in the
      internal storage notifications directory.  This happened to Leslie's S10 phone when she purchased or upgraded to Android 11.  This URI is also used to generate a ringtone object.
      In case this ringtone object turns out to be null I checked for that condition every place it is used.

2.074-noads-03-16-2021-1800 - versionName "2.074", versionCode 50.  Release name 2.074-noads-03-16-2021-1800.  Code is archived as 03_16_2021_1800_WSJTXMonitor.tgz.
  This was released to Internal Test.  Android 11 required a "query" entry in the manifest to specify which other packages on the device I wanted to use.  I added such
  a query in noads/AndroidManifest.xml to specify maps.  Without this entry the call to getPackageManager().getInstalledApplications() in BaseActivity.isOppositeAppInstalled()
  did not show com.google.android.apps.maps as required by MainActivity.isGoogleMapsInstalled().

2.076-noads-03-26-2021-1530, 2.076-ads-03-26-2021-1530 - versionName "2.076", versionCode 52.  Release name 2.076-noads-03-26-2021-1530, 2.076-ads-03-26-2021-1530.  Code
      is archived as 03_26_2021_1530_WSJTXMonitor.tgz.
  This incorporates the changes in versions 2.073 and 2.074.  In addition I changed page 4 of the setup instructions (and the associated Google-sites web page) to reflect
  that JTAlert forwarding feature now works.
  src\ads\res\values\strings.xml, src\ads\res\values-es\strings.xml, src\noads\res\values\strings.xml, and src\noads\res\values-es\strings.xml
    - update what's new text.
  UDPService.java
    - in onCreate placed the assignment to notificationUri in a try/catch block.  Since ringtone (variable) could be null I checked for it everywhere it is used.
      Also in a couple of existing try/catch blocks the log line didn't have the "*****" so I inserted them.
  src\main\res\values\strings.xml, ...\values-es\strings.xml, ...\values-ja\strings.xml, ...\values-pt\strings.xml, ...\values-ru\strings.xml, ...\values-zh\strings.xml
    - modified jtalert_header_text to reflect that JTAlert forwarding feature now works.
  AndroidManifest.xml
    - WAKE_LOCK permission added.  This is needed for the keep device awake feature.  It was apparently added automatically.
  noads\AndroidManifest.xml
    - Added a queries line.  Otherwise Android 11 would tell the user that Google Maps is not installed.
  build.gradle
    - compileSdkVersion and targetSdkVersion set to 30, versionCode and versionName updated.  Several libraries updated.

2.077-noads-05-17-2021-1405, 2.077-ads-05-17-2021-1405 - versionName "2.077", versionCode 53.  Release name 2.077-noads-05-17-2021-1405, 2.077-ads-05-17-2021-1405.  Code
      is archived as 05_17_2021_1405_WSJTXMonitor.tgz.
  This fixes a bug in that a six digit grid square with the last two digits in lower case was previously not recognized as valid.  JTDX forces the last two digits to lowercase.
    I actually added the check to the first two letters of the grid square as well, just in case a future version allows that as well.
  src\ads\res\values\strings.xml, src\ads\res\values-es\strings.xml, src\noads\res\values\strings.xml, and src\noads\res\values-es\strings.xml
    - update what's new text.
  HelperStationList.java
    - In function verifyGridSquare() I converted all four letters to uppercase before checking to see if they are valid.

2.088-noads-08-03-2021-1328 - versionName 2.088, versionCode 54.  Release name 2.088-noads-08-03-2021-1328.  Code is archived as 08_03_2021_1328_WSJTXMonitor.tgz.
    This was released to Internal Test.
  The only change here was to change the country names, adopted from cty.dat in WSJT-X source.  The code I used to parse this is on .48 machine at ~/java/CountriesList/.
  src\ads\res\values\strings.xml, src\ads\res\values-es\strings.xml, src\noads\res\values\strings.xml, and src\noads\res\values-es\strings.xml
    - update what's new text.
  src\release\java\com\feotec\wsjt_xmonitor\DebugUtils.java, src\debug\java\com\feotec\wsjt_xmonitor\DebugUtils.java
    - added a loggingFilters variable
  build.gradle (project)
    - updated gradle and google-services to 4.1.3 and 4.3.8 respectively.  This seemed to cause Android Studio to not install the app on any real device.
  build.gradle (app)
    - bumped versionName and versionCode.  Added compileOptions directive for Java version 8.  Updated library dependencies.  Added a dependency for multidex library
      and, related to that, added a "multiDexEnabled" statement in defaultConfig block.  Notes on multidex 6/28/2021.
  src\ads\res\values\string_array_init.xml, src\ads\res\values-es\string_array_init.xml
    - the big change.  major_countries_list_values was changed, replacing Russia with European Russia and Asiatic Russia, Great Britain replaced with England and Scotland.
      helper_countries_list_values changed from country list to prefix-country name-continent.  helper_countries_list_sorted_no_dups added, sorted list of countries with duplicates
      removed.  helper_countries_list_special_calls added, list of callsigns that don't fit the rules of helper_countries_list_values.  Also added update_country_names, a list
      of country names that have been changed, read by Filters.checkRemovedCountries().  Immediately below that are smaller string-arrays that list the replacement country names.
  HelperCountriesList.java
    - countriesList[] changed to ArrayList countriesArrayList and read from helper_countries_list_values at startup.  Added private class SpecialCalls and created another ArrayList
      of that class, specialCallsArrayList, which is read from helper_countries_list_special_calls at startup.  Also added a string array countriesArrayForFiltersAlerts[] which reads
      helper_countries_list_sorted_no_dups at startup.
    - getCountry() modified to first look for callsign in specialCallsArrayList.  If it doesn't find it then it will get the prefix and search through countriesArrayList as before, except instead of quitting at the first match it will continue looping through the list.  This is because countriesArrayList is
      sorted KH7K coming after KH7 whereas before it was sorted in the opposite manner.  So the last match will now be the most accurate.
    - getAllCountriesList() was modified to only read the data in helper_countries_list_sorted_no_dups.
  Filters.java
    - updateCountryNames() added.  Called from FiltersInterface.java when a previous filter is recalled.  It calls Filters.checkRemovedCountries(), a static method.  The purpose is to check if
      the filter contains countries that have been removed from the countries<> and substitute the new names.
    - checkRemovedCountries() added.  It is a static method (also called from Alerts.java).  It does all the work involved in looking for old countries and substituting new countries.
    - added DO_LOGGING variable and logInfo() method
  Alerts.java
    - updateCountryNames() added.  Functions just like the one in Filters.java.  Called when an alert is recalled.
  FiltersInterface.java
    - checkedItemsMajorCountries[] initialized in a different manner.  Since its length changed it made sense to do this better, in a manner not hard-coded.
    - in doNext() when a filter is reused or edited it first calls Filters.updateCountryNames() to check for removed countries.
  AlertsInterface.java
    - checkedItemsMajorCountries[] initialized in a different manner.  Since its length changed it made sense to do this better, in a manner not hard-coded.
    - in doNext() when an alert is reused or edited it first calls Alerts.updateCountryNames() to check for removed countries.

2.089-noads-09-09-2021-1140, 2.077-ads-09-09-2021-1140 - versionName "2.089", versionCode 55.  Release name 2.089-noads-09-09-2021-1140, 2.089-ads-09-09-2021-1140.  Code
      is archived as 09_09_2021_1140_WSJTXMonitor.tgz.
  This is a full release of the previous verions.  No other differences except versionName and versionCode.

2.090-noads-08-20-2023-1534, 2.090-ads-08-20-2023-1534 - versionName "2.090", versionCode 56.  Release name 2.090-noads-08-20-2023-1534, 2.090-ads-08-20-2023-1534.  Code
      is archived as 08-20-2023-1534_WSJTXMonitor.tgz.
  This is an internal test release.  Google required me to upgrade the app to support Android 13 (API 33).  In the process I installed the latest version of Android Studio, Gradle, etc.
  UDPService.java
    - In getNotification() added the flag PendingIntent.FLAG_IMMUTABLE to the constructor for activityPendingIntent.
  AndroidManifest.xml
    - package definition removed
    - An additional permission com.google.android.gms.permission.AD_ID
    - Added android:exported="true" to the activity for MainActivity
  noads/AndroidManifest.xml
    - package definition removed
  build.gradle
    - compileSdkVersion (deprecated) and targetSdkVersion bumped from 30 to 33
    - namespace 'com.feotec.wsjt_xmonitor' added at bottom of android{} block
    - updated all the dependencies to the latest except com.google.android.gms:play-services-ads:19.7.0
    - added implementation 'androidx.work:work-runtime-ktx:2.8.1' dependency to fix an Android Studio bug.
  build.gradle (WSJTXMonitor project version)
    - updated dependencies (classpath) to the latest versions.

2.091-noads-08-30-2023-2049, 2.091-ads-08-30-2023-2049 - versionName "2.091", versionCode 57.  Release name 2.091-noads-08-30-2023-2049, 2.091-ads-08-30-2023-2049.  Code
      is archived as 08-30-2023-xxxx_WSJTXMonitor.tgz.
  This is a full release of the previous verions.  No other differences except versionName and versionCode.

2.092-noads-09-13-2023-2227, 2.092-ads-09-13-2023-2227 - versionName "2.092", versionCode 58.  Release name 2.092-noads-09-13-2023-2227, 2.092-ads-09-13-2023-2227.  Code
      is archived as 09-13-2023-2227_WSJTXMonitor.tgz.
  When running v2.091 on Android 13 notifications (alerts) did not occur unless the app was currently displayed on the screen.  If another app was running or if the screen was locked
    then the alerts did not work.  The permission POST_NOTIFICATIONS needed to be added to the manifest file.
  This release is for internal test.

2.093-noads-09-14-2023-1953, 2.093-ads-03-14-2023-1953 - versionName "2.093", versionCode 59.  Release name 2.093-noads-09-14-2023-1953, 2.093-ads-09-14-2023-1953.
      Code is archived as 09-14-2023-1953_WSJTXMonitor.tgz.
  This is a full release of the previous verions.  No other differences except versionName and versionCode.

2.097-noads-08-05-2024-2250, 2.097-ads-08-05-2024-2250 - versionName "2.097", versionCode 62.  Release name 2.097-noads-08-05-2024-2250, 2.097-ads-08-05-2024-2250.
        Code is archived as 08-05-2024-2250_WSJTXMonitor.tgz.
    This is a release to internal test, both free and paid.  Google wanted me to upgrade to Android 14.  In the process of updating the libraries invoked in app/build.gradle
    I found I had to bump the minSdkVersion from 19 to 21 (Lollipop).  I previously removed the ads library from my build but never released it.
    I had hoped that I could make this my open source release - see WSJTX Monitor Notes.docx 8/2/2024
  AndroidManifest.xm.
    - Android 14 permissions included - FOREGROUND_SERVICE_CONNECTED_DEVICE, CHANGE_NETWORK_STATE, CHANGE_WIFI_STATE (the second two may have been part of my attempt
      to detect VPN).  In <service> block I added android:foregroundServiceType="connectedDevice", required by Android 14.
  app\build.gradle
    - lintOptions block changed to lint block, compileSdkVersion became compileSdk and moved into the defaultConfig block, minSdkVersion increased to 21 (Android 14).
      Also multiDexEnabled and associated library (com.android.support:multidex) removed since I'm no longer supporting versions before 21, Lollipop.  Bumped versionCode,
      versionName, targetSdkVersion.  And updated library versions.
  build.gradle
    - gradle version bumped to 8.2.2
  src\ads\res\values\bools.xml, src\noads\res\values\bools.xml
    - Removed doing_ads boolean
  src\ads\res\values\strings.xml, src\ads\res\values-es\strings.xml, app\src\noads\res\values\strings.xml, app\src\noads\res\values-es\strings.xml
    - Changed What's new message.
  src\debug\java\com\feotec\wsjt_xmonitor\DebugUtils.java, src\release\java\com\feotec\wsjt_xmonitor\DebugUtils.java
    - Removed ads related functions and variables - hashedDeviceId, handleAdRequestsAddTestDevice(), EEAConsentAddTestDevice(), and EEAConsentSetGeography().  Also
      removed the import statements for the three ad libraries.
  BaseActivity.java
    - Removed import statement for ads, doingAds variable, and a small block of code in onCreate() that read a resource and set doingAds.  Also doFinish() had an if
      version > 21 which was unnecessary.
      In addition to this there is a large block of code commented out in which I was attempting to determine if a VPN existed.  There are several import statements
      associated with that attempt (which are not commented out).
  DisplayAll.java
    - Removed more ad related code - an entire class handleAdRequests(), small snippets of code in onDestroy(), onPause(), onResume(), and onCreate() plus the AdView
      class instance and related import statements
  MainActivity.java
    - Nearly identical removals as DisplayAll.java.  In addition removed snipped of ad related code in onPrepareOptionsMenu() plus entire classes handleAdRequests(),
      collectConsent(), and EEAConsent().
  UDPService.java
    - Android 14 related changes - call to startForeground() in onUnbind() had an additional parameter, FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE, which was imported
      at the top of the file.
  activity_display_all.xml, activity_main.xml
    - Removed drawing of banner ad at the bottom of the display
  menu_main.xml
    - removed some commented out code that had previously been the ads consent
  ids.xml
    - removed and ads related action_eea_consent item.
  strings.xml, values-es\strings.xml, values-ja\strings.xml, values-pt\strings.xml, values-ru\strings.xml, values-zh\strings.xml
    - removed the 'Change Ad Selection' menu item, which was never displayed.

2.098-noads-08-06-2024-2100, 2.098-ads-08-06-2024-2100 - versionName "2.098", versionCode 63.  Release name 2.098-noads-08-06-2024-2100, 2.098-ads-08-06-2024-2100.
      Code is archived as 08-06-2024-2100_WSJTXMonitor.tgz.
  This is a full release of the previous verions.  No other differences except versionName and versionCode.

2.102-noads-08-30-2024-1851, 2.102-ads-08-30-2024-1851 - versionName "2.102", versionCode 64.  Release name 2.102-noads-08-30-2024-1851, 2.102-ads-08-30-2024-1851.
      Code is archived as 08-30-2024-1851_WSJTXMonitor.tgz.
    This is a release to internal test, both free and paid.  The purpose was to remove Firebase.  There were a few other small changes made, mostly preparing for open
    source release.
  signingConfigs.properties 
    - added to hold the passwords for GPlay, using in app version of build.gradle.  This MUST be included in .gitignore.  This was done to get them out of Git repository.
  app\google-services.json
    - removed.  This held the keys for Firebase.
  Firebase Config 
    - entire directory removed.  This was a collection of the keys for Firebase as I acquired them.
  app\ads\res\values\bools.xml 
    - removed, everything was commented out
  app\src\debug\res\values\strings.xml 
    - removed, contained admob debug keys
  app\src\release\res\values\strings.xml 
    - removed, contained release admob keys
  app\src\main\AndroidManifest.xml 
    - removed permission AD_ID, removed four meta-data elements related to Firebase.
  app\build.gradle 
    - removed a one dependency related to Firebase, removed a couple of commented out lines regarding multidex, in signingConfigs section I implemented code to load 
      keystore from signingConfigs.properties.  Also removed plugin com.google.gms.google-services.
  app\src\main\java\com\feotec\wsjt_xmonitor\Alerts.java
    - removed method describeAlertForFirebase(), called from AlertsInterface::analyticsAlerts().
  app\src\main\java\com\feotec\wsjt_xmonitor\AlertsInterface.java
    - removed Firebase code, thisFirebaseAnalytics object, code that assigned the apps Firebase object to thisFirebaseAnalytics, method analyticsAlerts() and the line
      that called it.
  app\src\main\java\com\feotec\wsjt_xmonitor\Filters.java
    - removed method describeFilterForFirebase(), called from FiltersInterface::analyticsFilters()
  app\src\main\java\com\feotec\wsjt_xmonitor\FiltersInterface.java
    - similar to AlertsInterface, removed Firebase code, thisFirebaseAnalytics object, code that assigned the apps Firebase object to thisFirebaseAnalytics, method 
      analyticsFilters() and the line that called it.
  app\src\main\java\com\feotec\wsjt_xmonitor\BaseActivity.java
    - removed blocks of code related to Firebase - the main object mFirebaseAnalytics, a class of constants supporting my needs in Firebase, code in onCreate() that acquires
      a Firebase instance, and then initializes it, code in dialogEULAOkClick() allowing Firebase data collection to be turned on and off, the method analyticsReportSetup()
      and several places where it is called.  Unrelated to Firebase, I also changed the menu switch statement to an if/elseif block, suggested by Google.
  app\src\main\java\com\feotec\wsjt_xmonitor\DialogEULA.java
    - removed code to allow the user to opt in or out of data collection
  app\src\main\java\com\feotec\wsjt_xmonitor\DisplayAll.java
    - unrelated to Firebase, wrapped call to setDisplayHomeAsUpEnabled() in check to make sure object is not null (to satisfy a lint warning).
  app\src\main\java\com\feotec\wsjt_xmonitor\MainActivity.java
    - removed Firebase code for number of replies in onCreateOptionsMenu(), Firebase code in onOptionsItemSelected() for number of mappings, Firebase code in 
      dialogSetRxFreqSelectionListener() the counts number of frequency selections, Firebase code in dialogSortingNextListener() to count sortings, two calls to 
      analyticsReportSetup(), and one each call to alertsInterface.setFirebaseAnalytics() and filtersInterface.setFirebaseAnalytics().  Unrelated to Firebase I also 
      changed the menu switch statement to an if/elseif block, suggested by Google.
  app\src\main\java\com\feotec\wsjt_xmonitor\WSJTXUtils.java
    - removed many constants and functions, mostly related to Firebase - KEY_NON_PERSONALIZED_ADS, KEY_NUM_SET_RX_SELECTIONS, KEY_NUM_MAPPINGS/SORTINGS/REPLIES,  
      getDataCollection(), setDataCollection(), getAndBumpNumberOfSetRxFreqSelections(), getNumberOfSetRxFreqSelections(), getNumberOfFilterUses(), getAndBumpNumberOfMappings()
      getNumberOfMappings(), getAndBumpNumberOfSortings(), getNumberOfSortings(), getAndBumpNumberOfReplies(), getNumberOfReplies(), getNumberOfAlertUses(), 
      getNonPersonalizedAds(), and setNonPersonalizedAds().
  
2.103-noads-09-24-2025-2000, 2.103-ads-09-24-2025-2000 - versionName "2.103", versionCode 65.  Release name 2.103-noads-09-24-2025-2000, 2.103-ads-09-24-2025-2000.
      Code is archived as 09-24-2025-2000_WSJTXMonitor.tgz.
    This is a release to internal test, both free and paid.  The purpose was to update to Android 15 and 16, SDK version 36.  
  src\ads\res\values\strings.xml, src\ads\res\values-es\strings.xml, app\src\noads\res\values\strings.xml, app\src\noads\res\values-es\strings.xml
    - Changed What's new message.

2.104-noads-09-24-2025-2200, 2.104-ads-09-24-2025-2200 - versionName "2.104", versionCode 66.  Release name 2.104-noads-09-24-2025-2200, 2.104-ads-09-24-2025-2200.
      Code is archived as 09-24-2025-2200_WSJTXMonitor.tgz.
    This is a production release, identical to v2.103.  The purpose was to update to Android 15 and 16, SDK version 36.  Only changed versionName and versionCode.

